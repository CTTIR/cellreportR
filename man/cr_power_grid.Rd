% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics-power.R
\name{cr_power_grid}
\alias{cr_power_grid}
\title{Sample sizes for a whole effect grid}
\usage{
cr_power_grid(
  effects,
  estimate = "cohens_d",
  ci_low = NULL,
  ci_high = NULL,
  available = NULL,
  power = 0.8,
  sig_level = 0.05,
  type = c("two.sample", "one.sample", "paired"),
  alternative = c("two.sided", "one.sided")
)
}
\arguments{
\item{effects}{An effect grid from \code{\link[=cr_effect_grid]{cr_effect_grid()}}, or any data
frame with an estimate column and its two interval columns.}

\item{estimate}{Name of the effect size column. The interval columns
default to \verb{<estimate>_ci_low} and \verb{<estimate>_ci_high}.}

\item{ci_low, ci_high}{Optional explicit names of the interval
columns.}

\item{available}{Units already available per arm, used to flag which
contrasts are already large enough. Either a column name in
\code{effects} or a numeric vector. Defaults to \code{pmin(n_ref, n_cmp)}
when those columns are present.}

\item{power}{Target power (default 0.8).}

\item{sig_level}{Significance level (default 0.05).}

\item{type}{Design passed to \code{\link[=cr_power]{cr_power()}}.}

\item{alternative}{Alternative passed to \code{\link[=cr_power]{cr_power()}}.}
}
\value{
\code{effects} with the columns \code{d_conservative}, \code{n_observed},
\code{n_conservative} and \code{basis} appended, plus \code{n_available} and
\code{sufficient} when availability is known.
}
\description{
Applies \code{\link[=cr_power]{cr_power()}} to every row of an effect grid produced by
\code{\link[=cr_effect_grid]{cr_effect_grid()}} and binds the sizing columns onto it, so the
observed and the conservative sample size travel with the contrast
they belong to.
}
\examples{
set.seed(1)
units <- data.frame(
  compound = rep(c("CompoundA", "CompoundB"), each = 12),
  arm = rep(rep(c("reference", "interval_short"), each = 6), 2),
  log2_fc = c(stats::rnorm(6, 0, 0.3), stats::rnorm(6, -1.4, 0.3),
              stats::rnorm(6, 0, 0.3), stats::rnorm(6, -0.1, 0.3))
)
eff <- cr_effect_grid(units, "log2_fc", "arm", "reference",
                      by = "compound")
sizes <- cr_power_grid(eff)
sizes[, c("compound", "n_observed", "n_conservative", "basis")]

}
\seealso{
\code{\link[=cr_power]{cr_power()}}, \code{\link[=cr_effect_grid]{cr_effect_grid()}}.

Other power:
\code{\link[=cr_conservative_effect]{cr_conservative_effect()}},
\code{\link[=cr_power]{cr_power()}},
\code{\link[=cr_power_analysis]{cr_power_analysis()}}
}
\concept{power}
