% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics-effects.R
\name{cr_effect_size}
\alias{cr_effect_size}
\title{Compute effect sizes between two samples}
\usage{
cr_effect_size(
  x,
  y,
  method = c("cohens_d", "hedges_g", "cliffs_delta", "rank_biserial", "glass_delta"),
  ci = 0.95,
  ci_method = c("analytic", "bootstrap"),
  n_boot = 200,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric vector (treatment group).}

\item{y}{Numeric vector (control / reference group).}

\item{method}{Character vector of methods to compute. Allowed:
\code{"cohens_d"}, \code{"hedges_g"}, \code{"cliffs_delta"},
\code{"rank_biserial"}, \code{"glass_delta"}.}

\item{ci}{Confidence level for the interval (default 0.95). Set
\code{NULL} to skip interval estimation.}

\item{ci_method}{\code{"analytic"} (default) or \code{"bootstrap"}.}

\item{n_boot}{Number of bootstrap resamples used when
\code{ci_method = "bootstrap"}.}

\item{seed}{Optional integer seed for the bootstrap. The global
random number stream is restored on exit.}
}
\value{
A tibble with columns \code{method}, \code{estimate}, \code{ci_low},
\code{ci_high} and \code{magnitude}.
}
\description{
Standardized effect sizes for a comparison of two independent
samples, each with a confidence interval. Confidence intervals are
analytic by default (deterministic and cheap on large samples); a
bootstrap interval is available for cases where the analytic
approximation is not wanted.
}
\details{
Cohen's \emph{d} uses the pooled standard deviation without small-sample
correction and its interval is the large-sample (Hedges-Olkin)
approximation with a \emph{t} quantile on \code{n_x + n_y - 2} degrees of
freedom. Hedges' \emph{g} applies the bias correction
\eqn{J = 1 - 3 / (4 \cdot df - 1)} to both estimate and interval.
Glass's delta standardizes by the standard deviation of \code{y} only.
Cliff's delta uses the consistent variance estimator with the
interval on the transformed scale, so the bounds always lie inside
\eqn{[-1, 1]}. The rank-biserial correlation of two independent
samples equals Cliff's delta and is reported on the same scale.
}
\examples{
set.seed(1)
cr_effect_size(stats::rnorm(100, 1), stats::rnorm(100, 0))

# bootstrap interval with a reproducible seed
cr_effect_size(stats::rnorm(50, 1), stats::rnorm(50, 0),
               method = "cliffs_delta",
               ci_method = "bootstrap", n_boot = 50, seed = 42)

}
\seealso{
\code{\link[=cr_effect_grid]{cr_effect_grid()}} for a whole grid of contrasts,
\code{\link[=cr_power]{cr_power()}} for the sample size implied by an effect size.

Other effect sizes:
\code{\link[=cr_compare_levels]{cr_compare_levels()}},
\code{\link[=cr_effect_grid]{cr_effect_grid()}}
}
\concept{effect sizes}
