% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{cr_normalize}
\alias{cr_normalize}
\title{Normalize intensity data}
\usage{
cr_normalize(
  experiment,
  channel,
  method = c("background", "control", "zscore", "robust_zscore", "quantile"),
  control_group = NULL,
  ...
)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{channel}{Channel column to normalize.}

\item{method}{One of \code{"background"}, \code{"control"}, \code{"zscore"},
\code{"robust_zscore"}, \code{"quantile"}.}

\item{control_group}{For \code{method = "control"}: the treatment name
(in \code{design$treatment}) whose median / mean is used as reference.}

\item{...}{Additional arguments passed to the underlying method.}
}
\value{
A modified \code{cr_experiment}.
}
\description{
Applies a channel-wise normalization. The normalized channel
values replace the original column in \code{cells}.
}
\details{
\itemize{
\item \code{"background"} subtracts per-well background via
\code{\link[=cr_background_subtract]{cr_background_subtract()}} (percentile method, 5th percentile).
\item \code{"control"} divides each cell's intensity by the median intensity
of cells assigned to \code{control_group}, then takes the log2 ratio.
\item \code{"zscore"} applies a global Z-score across all cells.
\item \code{"robust_zscore"} uses the median and MAD.
\item \code{"quantile"} maps the per-well empirical distributions onto the
global quantile distribution.
}
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 30)
cr_normalize(exp, channel = "marker_1", method = "robust_zscore")
}
