% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-screen.R
\name{cr_plot_forest}
\alias{cr_plot_forest}
\title{Forest plot of effect sizes with confidence intervals}
\usage{
cr_plot_forest(
  effects,
  estimate = "estimate",
  ci_low = "ci_low",
  ci_high = "ci_high",
  label = NULL,
  facet_by = NULL,
  colour_by = NULL,
  method = "cohens_d",
  reference = 0,
  order_by_estimate = TRUE,
  descending = FALSE,
  title = NULL,
  subtitle = NULL,
  x_lab = NULL,
  y_lab = NA
)
}
\arguments{
\item{effects}{A data frame of effect sizes (for example the output of an
effect-size grid), a \code{cr_result}, or a list of \code{cr_result}s.}

\item{estimate, ci_low, ci_high}{Names of the estimate and interval columns.}

\item{label}{Name of the column identifying each row. \code{NULL} (default)
picks the first of \code{group}, \code{compound}, \code{treatment}, \code{contrast}, \code{label}
or \code{term} that is present.}

\item{facet_by}{One or two column names to facet on, or \code{NULL}.}

\item{colour_by}{Column mapped to colour, fill and shape, or \code{NULL}.}

\item{method}{Effect-size method to keep when \code{effects} carries a \code{method}
column with several methods (default \code{"cohens_d"}).}

\item{reference}{Position of the null reference line (default \code{0}).}

\item{order_by_estimate}{Order rows by the estimate (default \code{TRUE}).
\code{FALSE} keeps the order of \code{label}.}

\item{descending}{Order largest estimate at the top (default \code{FALSE}, which
puts the most negative - typically the strongest protection - at the top).}

\item{title, subtitle, x_lab, y_lab}{Plot labels. \code{NULL} uses a computed
default; \code{NA} omits the label.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws one row per contrast: the point estimate with its confidence
interval, a reference line at the null, and the rows ordered by the
estimate rather than alphabetically, so the ordering is computed from the
data and cannot go stale.
}
\details{
The default subtitle states which intervals exclude the reference. It is
computed from the interval bounds for the same reason: the equivalent
sentence written by hand in the source pipeline stopped matching the data
the first time the data changed.
}
\examples{
effects <- data.frame(
  group = c("CompoundA", "CompoundB", "CompoundC"),
  estimate = c(-1.2, -0.35, 0.1),
  ci_low = c(-1.9, -0.95, -0.4),
  ci_high = c(-0.5, 0.25, 0.6)
)
cr_plot_forest(effects)
}
\seealso{
\code{\link[=cr_plot_screen]{cr_plot_screen()}}, \code{\link[=cr_plot_effect_sizes]{cr_plot_effect_sizes()}}

Other screen figures:
\code{\link[=cr_plot_qc_gate]{cr_plot_qc_gate()}},
\code{\link[=cr_plot_sample_size]{cr_plot_sample_size()}},
\code{\link[=cr_plot_screen]{cr_plot_screen()}},
\code{\link[=cr_plot_specificity]{cr_plot_specificity()}},
\code{\link[=cr_save_plot]{cr_save_plot()}}
}
\concept{screen figures}
