% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-macros.R
\name{cr_macros}
\alias{cr_macros}
\title{Emit named values as a generated include file}
\usage{
cr_macros(
  values,
  file,
  format = c("tex", "json", "yaml"),
  prefix = NULL,
  header = NULL,
  digits = 3,
  big_mark = ",",
  na = "--"
)
}
\arguments{
\item{values}{A named list or named atomic vector of scalars. Nested
named lists are flattened, joining the names.}

\item{file}{Output path. When \code{NULL} the formatted lines are
returned instead of being written.}

\item{format}{\code{"tex"} (default) writes \verb{\newcommand} definitions
with presentation-formatted values; \code{"json"} and \code{"yaml"} write
the underlying values, rounded but otherwise raw, for
downstream tools.}

\item{prefix}{Optional prefix prepended to every name. A prefix is
worth setting for \code{"tex"} output: it keeps generated names clear
of the commands LaTeX already defines, such as \verb{\\label} or
\verb{\\date}, which cannot be redefined with \verb{\newcommand}.}

\item{header}{Optional character vector of header lines. \code{NULL}
emits a default "generated file, do not edit" banner; \code{character()}
emits none.}

\item{digits}{Decimal places used when formatting numbers. Counts —
integers, and round values of 1000 or more — are written without
decimals and with \code{big_mark} separators.}

\item{big_mark}{Thousands separator used in \code{"tex"} output.}

\item{na}{Placeholder for non-finite values in \code{"tex"} output.}
}
\value{
The output path (invisibly), or a character vector of lines
when \code{file} is \code{NULL}.
}
\description{
Writes every number a write-up quotes into one machine-generated
file, so the text can reference the value by name instead of
repeating the digits. A number that lives in exactly one place
cannot disagree with itself: re-run the analysis, re-emit the file,
and the document is current. Splicing an estimate from one run
beside an interval from another stops being possible.
}
\details{
LaTeX control sequences may contain letters only, so names are
transliterated by \code{\link[=cr_macro_name]{cr_macro_name()}} — \code{"CompoundA_5min"} becomes
\verb{\\CompoundAfivemin}. Two source names that transliterate to the same
macro are an error rather than a silent overwrite.
}
\examples{
vals <- list(cells_analyzed = 128400L, units_analyzed = 96L,
             top_estimate = 1.42, top_ci_low = 0.55)
cat(cr_macros(vals, file = NULL), sep = "\n")

f <- tempfile(fileext = ".tex")
cr_macros(vals, f, prefix = "screen")
cat(readLines(f)[1:2], sep = "\n")
}
\seealso{
\code{\link[=cr_macros_from]{cr_macros_from()}} to derive the values from a report,
\code{\link[=cr_macro_name]{cr_macro_name()}}, \code{\link[=cr_format_number]{cr_format_number()}}, \code{\link[=cr_enumerate]{cr_enumerate()}}.

Other macros:
\code{\link[=cr_enumerate]{cr_enumerate()}},
\code{\link[=cr_format_number]{cr_format_number()}},
\code{\link[=cr_macro_name]{cr_macro_name()}},
\code{\link[=cr_macros_from]{cr_macros_from()}}
}
\concept{macros}
