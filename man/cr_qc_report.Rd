% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-audit.R
\name{cr_qc_report}
\alias{cr_qc_report}
\title{Report every analysis unit with its QC verdict}
\usage{
cr_qc_report(experiment, gate = NULL, unit = NULL, vars = NULL)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{gate}{Optional \code{cr_qc_gate}. Defaults to the gate stored by
\code{\link[=cr_apply_gate]{cr_apply_gate()}} in \code{metadata$qc_gate}, if any.}

\item{unit}{Analysis unit column. Defaults to the gate's unit, or
the experiment's unit / spatial unit.}

\item{vars}{Optional character vector of extra design columns to
carry into the report. \code{NULL} (default) carries the columns the
gate was batched on.}
}
\value{
A tibble with one row per unit: the unit identifier, the
requested design columns, \code{n_cells_gated} (cells the gate saw),
\code{n_cells} (cells present now), \code{retained}, and — when a gate is
available — the control statistics, \code{pct_of_control},
\code{fails_vs_median}, \code{fails_vs_mean}, \code{disputed}, \code{verdict} and
\code{reason}. Failing units are listed first.
}
\description{
Returns one row per analysis unit so that the gate can be audited:
which units were seen, what their signal was relative to their own
control, which were excluded and why, and how many cells each still
contributes. Units the gate removed are kept in the report — a gate
whose casualties disappear from the record cannot be reviewed.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 40)
gate <- cr_qc_gate(exp, "marker_1", "Untreated", batch_vars = "replicate")
exp2 <- cr_apply_gate(exp, gate)
rep <- cr_qc_report(exp2)
head(rep[, c("well", "n_cells", "verdict", "reason")])

# without a gate the report is still a per-unit inventory
head(cr_qc_report(exp))
}
\seealso{
\code{\link[=cr_qc_gate]{cr_qc_gate()}}, \code{\link[=cr_apply_gate]{cr_apply_gate()}}, \code{\link[=cr_qc_summary]{cr_qc_summary()}}.

Other quality control:
\code{\link[=cr_apply_gate]{cr_apply_gate()}},
\code{\link[=cr_balance_cells]{cr_balance_cells()}},
\code{\link[=cr_exclude_small]{cr_exclude_small()}},
\code{\link[=cr_qc_doublets]{cr_qc_doublets()}},
\code{\link[=cr_qc_filter]{cr_qc_filter()}},
\code{\link[=cr_qc_gate]{cr_qc_gate()}},
\code{\link[=cr_qc_gate_impact]{cr_qc_gate_impact()}},
\code{\link[=cr_qc_intensity]{cr_qc_intensity()}},
\code{\link[=cr_qc_manual]{cr_qc_manual()}},
\code{\link[=cr_qc_summary]{cr_qc_summary()}},
\code{\link[=print.cr_qc_gate]{print.cr_qc_gate()}}
}
\concept{quality control}
