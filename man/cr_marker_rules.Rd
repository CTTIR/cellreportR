% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-markers.R
\name{cr_marker_rules}
\alias{cr_marker_rules}
\title{Declare how parenthetical file-name markers are interpreted}
\usage{
cr_marker_rules(
  merge_unit = NULL,
  partial_plate = NULL,
  omitted_reagent = NULL,
  reacquisition = NULL,
  lot = NULL,
  capture_unknown = TRUE,
  ignore_case = TRUE
)
}
\arguments{
\item{merge_unit}{Regular expression marking a file as one half of a
two-pass acquisition, or \code{NULL}.}

\item{partial_plate}{Regular expression matched against the container
(directory) name, or \code{NULL}.}

\item{omitted_reagent}{Regular expression marking the specificity arm,
or \code{NULL}.}

\item{reacquisition}{Regular expression marking a repeated read, or
\code{NULL}.}

\item{lot}{Regular expression marking a reagent lot, or \code{NULL}.}

\item{capture_unknown}{Logical. Capture an unmatched trailing
parenthetical into \code{variant}. Default \code{TRUE}.}

\item{ignore_case}{Logical. Match case-insensitively. Default \code{TRUE}.}
}
\value{
An object of class \code{cr_marker_rules} (a list).
}
\description{
Acquisition software and operators annotate exports with short
markers, and those markers change the analysis. A \code{cr_marker_rules}
object states which regular expression means what, so that a marker
becomes a typed flag instead of a string somebody has to remember.
}
\details{
The distinctions the rules encode are deliberate:
\itemize{
\item \code{merge_unit} sits on a \strong{file} and means one spatial unit was
acquired in two passes. The two files are one unit and must be
merged before any per-unit balancing, or the unit contributes twice
the cells of its neighbours.
\item \code{partial_plate} sits on a \strong{container} (the plate directory) and
means a partly filled plate. It has no downstream consequence.
Collapsing the two into one flag hides the one that matters, so they
are matched against different strings.
\item \code{omitted_reagent} marks the specificity arm, where the detection
reagent was left out. These acquisitions are never samples.
\item \code{reacquisition} marks a repeated read of a unit that already has a
plain sibling file.
\item \code{lot} marks a different reagent lot. A lot marker is \emph{not} an
omitted-reagent control; pooling the two inverts the meaning of the
arm.
}

Any trailing parenthetical that matches none of the rules is captured
verbatim into a \code{variant} column when \code{capture_unknown = TRUE}, rather
than being guessed at or silently dropped.
}
\examples{
rules <- cr_marker_rules(
  merge_unit = "\\\\(split\\\\)",
  partial_plate = "\\\\(partial\\\\)",
  omitted_reagent = "\\\\(no reagent\\\\)",
  reacquisition = "\\\\(repeat\\\\)",
  lot = "\\\\(lot[A-Z]\\\\)"
)
rules
}
\seealso{
\code{\link[=cr_extract_markers]{cr_extract_markers()}}, \code{\link[=cr_parse_paths]{cr_parse_paths()}},
\code{\link[=cr_merge_rules]{cr_merge_rules()}}.

Other import:
\code{\link[=cr_assign_units]{cr_assign_units()}},
\code{\link[=cr_centroid_overlap]{cr_centroid_overlap()}},
\code{\link[=cr_column_map]{cr_column_map()}},
\code{\link[=cr_extract_markers]{cr_extract_markers()}},
\code{\link[=cr_filename_grammar]{cr_filename_grammar()}},
\code{\link[=cr_merge_rules]{cr_merge_rules()}},
\code{\link[=cr_parse_paths]{cr_parse_paths()}},
\code{\link[=cr_path_spec]{cr_path_spec()}},
\code{\link[=cr_read_cellprofiler]{cr_read_cellprofiler()}},
\code{\link[=cr_read_cells]{cr_read_cells()}},
\code{\link[=cr_read_design]{cr_read_design()}},
\code{\link[=cr_read_export]{cr_read_export()}},
\code{\link[=cr_read_exports]{cr_read_exports()}},
\code{\link[=cr_read_qupath]{cr_read_qupath()}},
\code{\link[=cr_read_segmantr]{cr_read_segmantr()}},
\code{\link[=cr_unit_map]{cr_unit_map()}}
}
\concept{import}
