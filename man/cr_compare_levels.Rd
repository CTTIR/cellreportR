% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics-grid.R
\name{cr_compare_levels}
\alias{cr_compare_levels}
\title{Compare unit-level and cell-level effect estimates}
\usage{
cr_compare_levels(unit_effects, cell_effects, by = NULL, estimate = "cohens_d")
}
\arguments{
\item{unit_effects}{Effect grid computed with \code{unit} set, from
\code{\link[=cr_effect_grid]{cr_effect_grid()}}.}

\item{cell_effects}{Effect grid computed on the same data without
\code{unit}.}

\item{by}{Join keys. \code{NULL} (default) uses every non-numeric column
the two grids share, which is the stratifying columns plus
\code{contrast}.}

\item{estimate}{Name of the effect size column to compare.}
}
\value{
A tibble with the join keys, \code{estimate_unit},
\code{estimate_cell}, \code{width_unit}, \code{width_cell} and \code{ratio}
(\code{width_unit / width_cell}). The median ratio is attached as the
\code{median_ratio} attribute.
}
\description{
Puts the two aggregation levels of a screen side by side and
quantifies how much narrower the cell-level intervals are. A ratio
well above one is pseudo-replication rather than precision: cells
within a unit are not independent, so the cell-level interval
understates the uncertainty of the very same estimate.
}
\examples{
set.seed(1)
cells <- data.frame(
  compound = rep(c("CompoundA", "CompoundB"), each = 240),
  arm = rep(rep(c("reference", "interval_short"), each = 120), 2),
  unit_id = rep(paste0("u", 1:8), each = 60),
  log2_fc = stats::rnorm(480)
)
cells$log2_fc <- cells$log2_fc +
  ifelse(cells$compound == "CompoundA" & cells$arm == "interval_short",
         -1, 0)
u <- cr_effect_grid(cells, "log2_fc", "arm", "reference",
                    by = "compound", unit = "unit_id")
k <- cr_effect_grid(cells, "log2_fc", "arm", "reference",
                    by = "compound", min_n = 10)
cmp <- cr_compare_levels(u, k)
cmp
attr(cmp, "median_ratio")

}
\seealso{
\code{\link[=cr_effect_grid]{cr_effect_grid()}}.

Other effect sizes:
\code{\link[=cr_effect_grid]{cr_effect_grid()}},
\code{\link[=cr_effect_size]{cr_effect_size()}}
}
\concept{effect sizes}
