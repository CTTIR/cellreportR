% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize-batch.R
\name{cr_standardize_batch}
\alias{cr_standardize_batch}
\title{Standardize a channel against the control of each cell's own batch}
\usage{
cr_standardize_batch(
  experiment,
  channel,
  control_level,
  batch_vars,
  control_var = "treatment",
  method = c("log2_fc", "zscore", "raw"),
  eps = 1,
  sd_floor = 1e-08,
  value_to = "value_std",
  on_missing_control = c("error", "warn", "drop")
)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{channel}{Channel column to standardize. Must be numeric.}

\item{control_level}{Value (or values) of \code{control_var} that mark
the control cells of a batch.}

\item{batch_vars}{Character vector of columns that jointly define a
batch. See \code{\link[=cr_batch_key]{cr_batch_key()}}.}

\item{control_var}{Column holding the treatment assignment.
Defaults to \code{"treatment"}.}

\item{method}{Which quantity becomes the canonical analysis value
in \code{value_to}: \code{"log2_fc"} (default), \code{"zscore"} (the per-batch
z-score \code{z_ctrl}), or \code{"raw"} (the untransformed channel, for a
like-for-like comparison of standardized against measured
signal).}

\item{eps}{Additive offset for the log2 fold change.}

\item{sd_floor}{Lower bound for the control standard deviation.}

\item{value_to}{Name of the column receiving the value selected by
\code{method}. Use \code{NULL} to add the component columns only.}

\item{on_missing_control}{What to do with batches that have no
control cells: \code{"error"} (default), \code{"warn"} or \code{"drop"}.}
}
\value{
The \code{cr_experiment} with these columns added to \code{cells}:
\describe{
\item{\code{batch_key}}{The batch each cell belongs to.}
\item{\code{ctrl_n}, \code{ctrl_mean}, \code{ctrl_median}, \code{ctrl_sd}}{The
reference statistics of that batch.}
\item{\code{z_ctrl}}{\code{(y - ctrl_mean) / ctrl_sd}.}
\item{\code{log2_fc}}{\code{log2((y + eps) / (ctrl_mean + eps))}.}
\item{\code{value_to}}{The value selected by \code{method}.}
}
The reference table is stored as \verb{$batch_reference}, the batch
columns as \verb{$batch_vars}, and the settings under
\verb{$metadata$standardization}.
}
\description{
Expresses every cell relative to the control cells of the batch it
belongs to, rather than to one pooled reference for the whole
experiment. This is the standardization a plate-based screen needs:
plates, runs and acquisition days each carry their own offset, and
a single pooled control folds that offset into the estimate.
}
\details{
Three quantities are always added, whichever \code{method} is chosen,
because downstream steps need different ones: \code{log2_fc}, \code{z_ctrl}
and the untouched channel. \code{method} only decides which of them is
copied into \code{value_to} as the canonical analysis value.

The log2 fold change uses an \emph{additive} offset,
\code{log2((y + eps) / (ctrl_mean + eps))}, not a floor on \code{y}. An
offset keeps the transformation monotone and well behaved near
zero, whereas clamping small values to a floor flattens a whole
range of the signal onto one number.

The fold change divides by the control \strong{mean} while
\code{\link[=cr_batch_reference]{cr_batch_reference()}} also reports the control \strong{median}; see
there for why both are kept.

A batch that contains no control cells cannot be standardized: its
reference does not exist, and a value computed against some other
batch's control would be a silent error rather than a measurement.
\code{cr_standardize_batch()} therefore refuses by default. Use
\code{on_missing_control = "drop"} to remove those cells (recorded in
the QC log) or \code{"warn"} to keep them with \code{NA} standardized values.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 20)
exp$design$plate <- rep(c("P1", "P2"), length.out = nrow(exp$design))

std <- cr_standardize_batch(exp, channel = "marker_1",
                            control_level = "Untreated",
                            batch_vars = "plate")
round(summary(std$cells$log2_fc), 2)
std$batch_reference

# Per-batch z-score instead of the fold change
z <- cr_standardize_batch(exp, channel = "marker_1",
                          control_level = "Untreated",
                          batch_vars = "plate", method = "zscore")
round(stats::median(z$cells$value_std), 2)

# A batch without control cells is refused, not guessed at
exp$design$plate[exp$design$treatment == "Untreated"] <- "P1"
try(cr_standardize_batch(exp, channel = "marker_1",
                         control_level = "Untreated",
                         batch_vars = "plate"))
}
\seealso{
\code{\link[=cr_batch_reference]{cr_batch_reference()}}, \code{\link[=cr_batch_key]{cr_batch_key()}}, \code{\link[=cr_normalize]{cr_normalize()}}

Other batch standardization functions:
\code{\link[=cr_batch_key]{cr_batch_key()}},
\code{\link[=cr_batch_reference]{cr_batch_reference()}},
\code{\link[=cr_correct_batch]{cr_correct_batch()}}
}
\concept{batch standardization functions}
