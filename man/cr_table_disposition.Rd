% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantify.R
\name{cr_table_disposition}
\alias{cr_table_disposition}
\title{Tabulate how many units and cells entered the analysis}
\usage{
cr_table_disposition(experiment, by = NULL, unit = NULL, total = TRUE)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{by}{Character vector of grouping columns from \code{design} or
\code{cells}. Defaults to \code{"treatment"} when that column exists, and to
no grouping otherwise.}

\item{unit}{Optional name of the column that identifies the
analysis unit. May be a column of \code{cells} or of \code{design}. If
\code{NULL} (default) the experiment's \code{unit_var} slot is used when
present, and the spatial unit otherwise.}

\item{total}{Whether to append a row holding the overall counts. The
grouping columns of that row carry the label \code{"(total)"}.}
}
\value{
A tibble with the grouping columns (as character), \code{n_units},
\code{n_cells} and \code{median_cells_per_unit}.
}
\description{
Counts of analysis units and cells, overall and per arm. The
reported counts of an assay are themselves an analysis output: once
they are generated from the object that was analyzed, a count in the
write-up cannot drift away from the data it describes.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 20)
cr_table_disposition(exp)
cr_table_disposition(exp, by = "group")
}
\seealso{
\code{\link[=cr_tables]{cr_tables()}}, \code{\link[=cr_macros]{cr_macros()}}.

Other quantification:
\code{\link[=cr_compute_metrics]{cr_compute_metrics()}},
\code{\link[=cr_fold_change]{cr_fold_change()}},
\code{\link[=cr_summarize_wells]{cr_summarize_wells()}}
}
\concept{quantification}
