% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-macros.R
\name{cr_macros_from}
\alias{cr_macros_from}
\title{Derive generated numbers from a report or a results table}
\usage{
cr_macros_from(x, file, label_cols = NULL, ci = c("ci_low", "ci_high"), ...)
}
\arguments{
\item{x}{A \code{cr_report}, or a data frame with one row per contrast.}

\item{file}{Output path, or \code{NULL} to return the lines.}

\item{label_cols}{Character vector naming the columns that identify
a row. \code{NULL} (default) picks the first available of \code{group},
\code{compound}, \code{treatment}, \code{term}, \code{name}, together with \code{contrast}
or \code{comparison} when present.}

\item{ci}{Names of the lower and upper confidence-bound columns,
used for the "excludes zero" enumerations. \code{NULL} disables them.}

\item{...}{Passed to \code{\link[=cr_macros]{cr_macros()}} (\code{format}, \code{prefix}, \code{digits}, ...).}
}
\value{
The output path (invisibly), or a character vector of lines
when \code{file} is \code{NULL}.
}
\description{
Walks an assembled report (or any table of contrasts) and turns it
into the named values \code{\link[=cr_macros]{cr_macros()}} emits: the disposition counts,
one entry per contrast and numeric column, and the enumerations that
a sentence would otherwise state by hand — which intervals exclude
the null, and how many. Enumerations are the entries most worth
generating, because a hand-typed list is wrong as soon as one row of
the analysis changes.
}
\examples{
eff <- data.frame(
  group = c("CompoundA", "CompoundB", "CompoundC"),
  estimate = c(1.42, 0.31, -0.88),
  ci_low = c(0.55, -0.10, -1.60),
  ci_high = c(2.29, 0.72, -0.16)
)
cat(cr_macros_from(eff, file = NULL), sep = "\n")
}
\seealso{
\code{\link[=cr_macros]{cr_macros()}}.

Other macros:
\code{\link[=cr_enumerate]{cr_enumerate()}},
\code{\link[=cr_format_number]{cr_format_number()}},
\code{\link[=cr_macro_name]{cr_macro_name()}},
\code{\link[=cr_macros]{cr_macros()}}
}
\concept{macros}
