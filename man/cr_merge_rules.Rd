% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-units.R
\name{cr_merge_rules}
\alias{cr_merge_rules}
\alias{print.cr_merge_rules}
\title{Declare how files are merged into analysis units}
\usage{
cr_merge_rules(
  merge_suffix = "\\\\.1$",
  merge_marker = "merge_unit",
  keep_separate = "\\\\.2$",
  reacquisition = "reacquisition",
  merge_reacquisition = TRUE,
  separate_suffix = "re"
)

\method{print}{cr_merge_rules}(x, ...)
}
\arguments{
\item{merge_suffix}{Regular expression matched against the replicate
index, marking a second acquisition pass. \code{NULL} disables.}

\item{merge_marker}{Name of a logical column that must be \code{TRUE} for a
\code{merge_suffix} match to merge, or \code{NULL} to merge on the suffix
alone. When the column is absent no suffix merging happens.}

\item{keep_separate}{Regular expression matched against the replicate
index, marking indices that must never merge. \code{NULL} disables.}

\item{reacquisition}{Name of a logical column marking repeated reads,
or \code{NULL}.}

\item{merge_reacquisition}{Logical. Fold repeated reads into their
plain sibling. Default \code{TRUE}.}

\item{separate_suffix}{Suffix appended to the replicate index of a
repeated read when \code{merge_reacquisition = FALSE}. Default \code{"re"}.}

\item{x}{A \code{cr_merge_rules}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{cr_merge_rules} (a list).

\code{x}, invisibly.
}
\description{
States which replicate suffixes and marker flags mean "these files are
one unit" and which mean "these files are different units". Getting
this wrong is silent in both directions, so each rule is explicit.
}
\details{
\itemize{
\item \code{merge_suffix} matches the replicate index of a file that is the
\emph{second pass} over a unit that already exists. Left unmerged, such a
unit draws a full cell allocation twice and is double-weighted in
its own batch.
\item \code{merge_marker} names a logical column (typically produced by
\code{\link[=cr_extract_markers]{cr_extract_markers()}}) that must also be \code{TRUE} before a suffix
match is merged. Requiring the marker is the conservative default:
a suffix on its own is not evidence.
\item \code{keep_separate} matches replicate indices that look mergeable but
are a different physical unit. It always wins over \code{merge_suffix}.
\code{\link[=cr_centroid_overlap]{cr_centroid_overlap()}} is the evidence test for deciding which of
the two a given suffix is.
\item \code{reacquisition} names the logical column marking a repeated read.
With \code{merge_reacquisition = TRUE} such a file folds into its plain
sibling; otherwise \code{separate_suffix} is appended so it stays a unit
of its own.
}
}
\examples{
cr_merge_rules()
cr_merge_rules(merge_marker = NULL, keep_separate = NULL)
}
\seealso{
\code{\link[=cr_assign_units]{cr_assign_units()}}, \code{\link[=cr_extract_markers]{cr_extract_markers()}},
\code{\link[=cr_centroid_overlap]{cr_centroid_overlap()}}.

Other import:
\code{\link[=cr_assign_units]{cr_assign_units()}},
\code{\link[=cr_centroid_overlap]{cr_centroid_overlap()}},
\code{\link[=cr_column_map]{cr_column_map()}},
\code{\link[=cr_extract_markers]{cr_extract_markers()}},
\code{\link[=cr_filename_grammar]{cr_filename_grammar()}},
\code{\link[=cr_marker_rules]{cr_marker_rules()}},
\code{\link[=cr_parse_paths]{cr_parse_paths()}},
\code{\link[=cr_path_spec]{cr_path_spec()}},
\code{\link[=cr_read_cellprofiler]{cr_read_cellprofiler()}},
\code{\link[=cr_read_cells]{cr_read_cells()}},
\code{\link[=cr_read_design]{cr_read_design()}},
\code{\link[=cr_read_export]{cr_read_export()}},
\code{\link[=cr_read_exports]{cr_read_exports()}},
\code{\link[=cr_read_qupath]{cr_read_qupath()}},
\code{\link[=cr_read_segmantr]{cr_read_segmantr()}},
\code{\link[=cr_unit_map]{cr_unit_map()}}
}
\concept{import}
