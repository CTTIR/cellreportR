% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{cr_column_map}
\alias{cr_column_map}
\alias{print.cr_column_map}
\title{Declare a column contract for vendor exports}
\usage{
cr_column_map(exact = NULL, prefix = NULL, keep = NULL)

\method{print}{cr_column_map}(x, ...)
}
\arguments{
\item{exact}{Named character vector. Names are raw header names,
values the analysis name to rename them to.}

\item{prefix}{Named character vector. Names are regular expressions
matched against the raw header names, values the analysis name. The
first matching column is renamed.}

\item{keep}{Optional character vector of analysis names to retain
after renaming, in this order. Names that are absent are ignored.
\code{NULL} keeps every column.}

\item{x}{A \code{cr_column_map}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{cr_column_map}: a list with elements
\code{exact}, \code{prefix} and \code{keep}.

\code{x}, invisibly.
}
\description{
Segmented single-cell exports name their columns however the
acquisition software happens to. A \code{cr_column_map} records how those
raw headers translate to the analysis names used downstream, so that
the mapping lives in one declarative object instead of being spread
through reader code.
}
\details{
Two matching modes are available because vendor headers are not
always stable: \code{exact} matches a header verbatim, while \code{prefix}
matches a regular expression. Prefix matching exists for headers that
embed a unit glyph (an area column ending in a squared-micrometre
symbol, for instance), where the exact spelling cannot be relied on.

Renaming is deliberately \emph{tolerant}: names that do not occur in a
given export are skipped silently rather than raising an error. Exports
from the same instrument routinely carry different subsets of the
available measurements, and a strict renamer forces a second reader to
exist for every subset.
}
\examples{
map <- cr_column_map(
  exact = c(
    "Event Label" = "cell_id",
    "Signal - Mean Intensity" = "target_signal"
  ),
  prefix = c("^Nuclei - Area" = "area"),
  keep = c("cell_id", "target_signal", "area")
)
map
}
\seealso{
\code{\link[=cr_read_export]{cr_read_export()}}, \code{\link[=cr_read_exports]{cr_read_exports()}}.

Other import:
\code{\link[=cr_assign_units]{cr_assign_units()}},
\code{\link[=cr_centroid_overlap]{cr_centroid_overlap()}},
\code{\link[=cr_extract_markers]{cr_extract_markers()}},
\code{\link[=cr_filename_grammar]{cr_filename_grammar()}},
\code{\link[=cr_marker_rules]{cr_marker_rules()}},
\code{\link[=cr_merge_rules]{cr_merge_rules()}},
\code{\link[=cr_parse_paths]{cr_parse_paths()}},
\code{\link[=cr_path_spec]{cr_path_spec()}},
\code{\link[=cr_read_cellprofiler]{cr_read_cellprofiler()}},
\code{\link[=cr_read_cells]{cr_read_cells()}},
\code{\link[=cr_read_design]{cr_read_design()}},
\code{\link[=cr_read_export]{cr_read_export()}},
\code{\link[=cr_read_exports]{cr_read_exports()}},
\code{\link[=cr_read_qupath]{cr_read_qupath()}},
\code{\link[=cr_read_segmantr]{cr_read_segmantr()}},
\code{\link[=cr_unit_map]{cr_unit_map()}}
}
\concept{import}
