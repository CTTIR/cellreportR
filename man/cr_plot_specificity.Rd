% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-diagnostics.R
\name{cr_plot_specificity}
\alias{cr_plot_specificity}
\title{Specificity control plot}
\usage{
cr_plot_specificity(
  spec,
  arm = "arm",
  value = NULL,
  arm_levels = NULL,
  colour_by = NULL,
  log_y = TRUE,
  ratio_arms = NULL,
  title = NULL,
  subtitle = NULL,
  x_lab = NA,
  y_lab = NULL
)
}
\arguments{
\item{spec}{Cell-level or per-arm data: a data frame, or a list carrying
one in a \code{spec} or \code{specificity} element.}

\item{arm}{Name of the column identifying the arm (default \code{"arm"}).}

\item{value}{Name of the value column. \code{NULL} (default) auto-detects:
\code{median_signal} for a summarised table, otherwise \code{value}.}

\item{arm_levels}{Optional character vector fixing the order of the arms.}

\item{colour_by}{Column mapped to fill, or \code{NULL} to fill by arm.}

\item{log_y}{Draw the signal axis on a log scale (default \code{TRUE}).}

\item{ratio_arms}{Optional length-2 character vector naming the arms whose
median ratio is reported in the subtitle, as
\code{c(numerator, denominator)}. When \code{spec} carries a
\code{signal_to_background} attribute that value is used instead.}

\item{title, subtitle, x_lab, y_lab}{Plot labels. \code{NULL} uses a computed
default; \code{NA} omits the label.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the arms of the specificity control side by side: the assay with the
detection reagent present against the same exposure with the reagent
omitted. No amount of batch standardisation can show that a readout is
reagent-dependent rather than background autofluorescence, which is what
makes this the strongest validation a method-establishment result has.
}
\details{
Accepts either cell-level data (one row per cell, drawn as violins with the
arm medians annotated) or an already-summarised table (one row per arm,
drawn as columns).
}
\examples{
set.seed(1)
spec <- data.frame(
  arm = rep(c("reagent omitted\n+ vehicle", "reagent omitted\n+ exposed",
              "reagent present\n+ vehicle", "reagent present\n+ exposed"),
            each = 60),
  value = c(rlnorm(60, 3, 0.3), rlnorm(60, 3.05, 0.3),
            rlnorm(60, 4.5, 0.3), rlnorm(60, 6.2, 0.3))
)
cr_plot_specificity(spec)
}
\seealso{
Other screen figures:
\code{\link[=cr_plot_forest]{cr_plot_forest()}},
\code{\link[=cr_plot_qc_gate]{cr_plot_qc_gate()}},
\code{\link[=cr_plot_sample_size]{cr_plot_sample_size()}},
\code{\link[=cr_plot_screen]{cr_plot_screen()}},
\code{\link[=cr_save_plot]{cr_save_plot()}}
}
\concept{screen figures}
