% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-screen.R
\name{cr_example_screen}
\alias{cr_example_screen}
\title{Generate a synthetic multi-compound screen}
\usage{
cr_example_screen(
  seed = 42,
  n_compounds = 10,
  n_cells_per_well = 40,
  n_units_per_arm = 3,
  n_experiments = 2
)
}
\arguments{
\item{seed}{Random seed. \code{NULL} uses the current RNG state.}

\item{n_compounds}{Number of compounds, 1 to 10. They are named
\code{CompoundA} to \code{CompoundJ}.}

\item{n_cells_per_well}{Mean number of cells per unit (Poisson, with a
floor of ten).}

\item{n_units_per_arm}{Units per compound, experiment, interval and
exposure level. Three or more is needed for the effect-size grid.}

\item{n_experiments}{Number of experiments, 1 or 2.}
}
\value{
A \code{cr_experiment} with
\describe{
\item{\code{cells}}{one row per cell: \code{cell_id}, \code{well_id},
\code{source_file}, \code{source_path}, \code{x}, \code{y}, \code{area}, \code{circularity},
\code{nuclear_signal}, \code{target_signal}.}
\item{\code{design}}{one row per unit: \code{well_id}, \code{compound},
\code{experiment}, \code{plate}, \code{well}, \code{interval}, \code{dose}, \code{dose_unit},
\code{treatment}, \code{group}, \code{replicate}.}
\item{\code{provenance}}{one row per acquisition file, with marker
flags and the number of files each unit was assembled from.}
\item{\code{set_aside}}{a list whose \code{reagent_omitted} element holds
the specificity arm.}
}
\code{unit_var} is \code{"well_id"} and \code{batch_vars} is
\code{c("compound", "experiment", "plate", "interval")}.
}
\description{
Simulates a screen of up to ten compounds, each acquired at four
exposure levels and two pre-treatment intervals, on two plates and in
two experiments. Unlike \code{\link[=cr_example_experiment]{cr_example_experiment()}} the analysis unit is
not the raw plate well: units carry file provenance, some are
assembled from more than one acquisition, and one arm is set aside
instead of screened.
}
\details{
Everything the screening functions need is present: a vehicle control
inside every batch for \code{\link[=cr_batch_reference]{cr_batch_reference()}} and
\code{\link[=cr_standardize_batch]{cr_standardize_batch()}}, one unit that fails a control-referenced
gate for \code{\link[=cr_qc_gate]{cr_qc_gate()}}, enough units per arm for \code{\link[=cr_effect_grid]{cr_effect_grid()}},
plates crossed with arms for \code{\link[=cr_blocked_effect]{cr_blocked_effect()}}, and a
reagent-omitted arm in \code{set_aside}.
}
\section{Generating model}{

A batch is the combination of \code{compound}, \code{experiment}, \code{plate} and
\code{interval}. Writing \eqn{p_c} for the potency of compound \eqn{c},
\eqn{d} for the exposure level and \eqn{u_w} for the unit intercept:

\describe{
\item{potency}{\eqn{p_c} runs from \code{1.6} down to \code{0.1} in equal
steps from \code{CompoundA} to the last compound, so the rank order of
the screen is known in advance and the top hit is a hit by
construction rather than by chance.}
\item{exposure}{The response saturates in dose as
\eqn{d / (d + 40)}, so the four exposure levels are not equally
spaced in effect.}
\item{interval}{The longer pre-treatment interval multiplies the
effect by \code{1.15}.}
\item{effect}{\eqn{e = p_c \cdot d/(d + 40) \cdot g_{interval}}, in
log2 units against the vehicle of the same batch.}
\item{unit intercept}{\eqn{u_w \sim N(0, 0.12^2)} on the log scale,
one draw per unit.}
\item{acquisition offsets}{The second experiment has a \code{1.35} times
higher raw baseline and the second plate a \code{1.08} times higher
one. Both act on control and treated units alike, which is what
standardising against the control of a unit's own batch removes,
and why raw signal is not comparable across experiments.}
\item{target signal}{\eqn{\mathrm{LogNormal}(\log(400 \cdot g_{exp}
    \cdot g_{plate}) + u_w + e \log 2,\ 0.45^2)}.}
\item{nuclear signal}{\eqn{\mathrm{LogNormal}(\log 600 + u_w,\
    0.3^2)}, with no treatment effect.}
\item{morphology}{Nuclear \code{area} is
\eqn{\mathrm{LogNormal}(\log 180,\ 0.3^2)} and \code{circularity} is
\eqn{\mathrm{Beta}(6, 2)}. Four per cent of objects are shrunk to
an eighth of their area, the sub-threshold debris that
\code{\link[=cr_exclude_small]{cr_exclude_small()}} is meant to drop.}
}
}

\section{Planted edge cases}{

\describe{
\item{gate failure}{One treated unit of the first compound is given
a negative effect, so it sits below the control of its own batch
and fails a control-referenced gate.}
\item{two-pass acquisition}{One unit's cells are split across two
files, the second carrying a \code{(split)} marker and a \code{.1}
replicate suffix. Left unmerged it would count twice.}
\item{repeated read}{One unit's cells are split across a file and
its \verb{(repeat)} sibling, which has to merge into it.}
\item{look-alike suffix}{One separate unit is named with a \code{.2}
replicate suffix, which must \emph{not} merge with the unit named \code{2}:
it is a different physical unit on a different plate.}
\item{arm outside the screen}{A reagent-omitted arm is generated and
placed in \code{set_aside} rather than in the analysis pool.}
}

The random number generator state of the caller is restored on exit.
}

\examples{
screen <- cr_example_screen(seed = 1, n_compounds = 3,
                            n_cells_per_well = 12)
screen
table(screen$design$compound, screen$design$treatment)

# Units assembled from more than one acquisition file
prov <- screen$provenance
unique(prov$well_id[prov$n_files > 1])
}
\seealso{
\code{\link[=cr_example_experiment]{cr_example_experiment()}}, \code{\link[=cr_example_exports]{cr_example_exports()}},
\code{\link[=cr_batch_reference]{cr_batch_reference()}}, \code{\link[=cr_effect_grid]{cr_effect_grid()}}.

Other example data:
\code{\link[=cr_example_design]{cr_example_design()}},
\code{\link[=cr_example_experiment]{cr_example_experiment()}},
\code{\link[=cr_example_exports]{cr_example_exports()}},
\code{\link[=cr_example_files]{cr_example_files()}},
\code{\link[=cr_example_path]{cr_example_path()}}
}
\concept{example data}
