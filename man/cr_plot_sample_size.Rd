% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-diagnostics.R
\name{cr_plot_sample_size}
\alias{cr_plot_sample_size}
\title{Sample-size comparison plot}
\usage{
cr_plot_sample_size(
  sizes,
  label = NULL,
  observed = "n_observed",
  conservative = "n_conservative",
  available = NULL,
  colour_by = NULL,
  log_y = TRUE,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  x_lab = NA,
  y_lab = NULL
)
}
\arguments{
\item{sizes}{A data frame with one row per group, carrying the two
sample-size columns.}

\item{label}{Name of the grouping column. \code{NULL} (default) picks the first
of \code{group}, \code{compound}, \code{treatment} or \code{label} that is present.}

\item{observed, conservative}{Names of the two sample-size columns.}

\item{available}{Optional name of a column giving the units already
available per group; folded into the axis labels in brackets.}

\item{colour_by}{Column mapped to fill, or \code{NULL} for a single colour.}

\item{log_y}{Draw the count axis on a log scale (default \code{TRUE}, because
the two bars routinely differ by an order of magnitude).}

\item{title, subtitle, caption, x_lab, y_lab}{Plot labels. \code{NULL} uses a
computed default; \code{NA} omits the label.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws, per group, the number of units a confirmatory study would need when
powered on the observed effect beside the number needed when powered on the
confidence bound nearer the null.
}
\details{
The gap between the two bars is the point of the figure. Powering on the
observed effect of a screen's top hit is circular - that group is the
largest only because it was selected for being largest - so the
conservative bar is the one a confirmatory design should be built on, and a
conservative figure exists only where the interval excludes the null.

Value labels are drawn in ink above the bars, never inside them, and the
number of units already available is folded into the axis label rather than
plotted as a marker that collides with the labels on short bars.
}
\examples{
sizes <- data.frame(
  group = c("CompoundA", "CompoundB", "CompoundC"),
  n_observed = c(12, 84, 640),
  n_conservative = c(46, NA, NA),
  n_available = c(6, 6, 6)
)
cr_plot_sample_size(sizes, available = "n_available")
}
\seealso{
\code{\link[=cr_plot_forest]{cr_plot_forest()}}

Other screen figures:
\code{\link[=cr_plot_forest]{cr_plot_forest()}},
\code{\link[=cr_plot_qc_gate]{cr_plot_qc_gate()}},
\code{\link[=cr_plot_screen]{cr_plot_screen()}},
\code{\link[=cr_plot_specificity]{cr_plot_specificity()}},
\code{\link[=cr_save_plot]{cr_save_plot()}}
}
\concept{screen figures}
