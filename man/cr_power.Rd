% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics-power.R
\name{cr_power}
\alias{cr_power}
\title{Sample size for a future study, sized twice}
\usage{
cr_power(
  effect_size = NULL,
  ci_low = NULL,
  ci_high = NULL,
  power = 0.8,
  sig_level = 0.05,
  type = c("two.sample", "one.sample", "paired"),
  alternative = c("two.sided", "one.sided")
)
}
\arguments{
\item{effect_size}{Numeric vector of observed standardized effects
(Cohen's \emph{d}). May be \code{NULL} when only interval bounds are given.}

\item{ci_low, ci_high}{Numeric vectors of interval bounds for the
same effects. Optional.}

\item{power}{Target power (default 0.8).}

\item{sig_level}{Significance level (default 0.05).}

\item{type}{Design: \code{"two.sample"} (default), \code{"one.sample"} or
\code{"paired"}.}

\item{alternative}{\code{"two.sided"} (default) or \code{"one.sided"}.}
}
\value{
A tibble with one row per effect: \code{d_observed},
\code{n_observed}, \code{d_conservative}, \code{n_conservative}, \code{basis},
\code{power} and \code{sig_level}. Sample sizes are units per arm, rounded
up.
}
\description{
Solves the two-sample design for the number of units per arm needed
to reach \code{power} at \code{sig_level}, once for the observed effect and
once for the confidence bound nearer the null.
}
\details{
The conservative figure is the reportable one. Powering a screen's
leading compound on its own point estimate is circular: that
compound is the largest of the set only by virtue of having been
selected for being largest, so a sample size derived from it can
hardly fail to be met. \code{n_conservative} is \code{NA} whenever the
interval spans the null, because an interval compatible with no
effect cannot be sized.

The solver is \code{\link[stats:power.t.test]{stats::power.t.test()}}, so the effect size is read as
a standardized mean difference and \code{n} is per group.
}
\examples{
cr_power(effect_size = c(-1.31, -0.28),
         ci_low = c(-2.35, -1.20),
         ci_high = c(-0.27, 0.64))

# without an interval only the (circular) observed sizing is possible
cr_power(effect_size = 0.8)

}
\seealso{
\code{\link[=cr_power_grid]{cr_power_grid()}}, \code{\link[=cr_conservative_effect]{cr_conservative_effect()}},
\code{\link[=cr_power_analysis]{cr_power_analysis()}}.

Other power:
\code{\link[=cr_conservative_effect]{cr_conservative_effect()}},
\code{\link[=cr_power_analysis]{cr_power_analysis()}},
\code{\link[=cr_power_grid]{cr_power_grid()}}
}
\concept{power}
