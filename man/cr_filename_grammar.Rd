% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-grammar.R
\name{cr_filename_grammar}
\alias{cr_filename_grammar}
\alias{print.cr_filename_grammar}
\title{Declare the token grammar of an export file name}
\usage{
cr_filename_grammar(
  tokens = list(),
  defaults = list(),
  core_patterns = character(),
  typo_fixes = character(),
  prefix_strip = character(),
  replicate = "[0-9]+(?:\\\\.[0-9]+)?",
  sep = "_",
  normalise_space = TRUE
)

\method{print}{cr_filename_grammar}(x, ...)
}
\arguments{
\item{tokens}{Named list (or named character vector) of regular
expressions. Each is searched for in the name core; the matched text
becomes the value of a column named after the list element.}

\item{defaults}{Named list of values to use when a token is absent.
Elements with no entry default to \code{NA}.}

\item{core_patterns}{Character vector of regular expressions. The core
(the name with extension, markers, replicate index and prefix
removed) must match at least one of them. Empty means no whitelist.}

\item{typo_fixes}{Named character vector of \code{pattern = replacement}
repairs applied before anything else is parsed.}

\item{prefix_strip}{Character vector of leading labels to strip from
the core, longest first.}

\item{replicate}{Regular expression for the trailing replicate token.
Default \code{"[0-9]+(?:\\\\.[0-9]+)?"}, which accepts both \code{1} and \code{1.1}.}

\item{sep}{Token separator. Default \code{"_"}.}

\item{normalise_space}{Logical. Convert runs of whitespace to \code{sep}
and collapse repeated separators. Default \code{TRUE}.}

\item{x}{A \code{cr_filename_grammar}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{cr_filename_grammar} (a list).

\code{x}, invisibly.
}
\description{
A \code{cr_filename_grammar} states how one export file name decomposes
into design facts: which trailing token is the replicate index, which
leading labels are stripped, which substrings are tokens, and which
whole-name shapes are legal at all.
}
\details{
The \code{core_patterns} whitelist exists because the absence of a token is
itself meaningful — a name with no interval token is the untreated
reference arm, a name with no concentration token is a vehicle
control. A mistyped token therefore matches nothing, falls through to
the defaults, and silently reclassifies a treated unit as a vehicle
control, pulling it into the control denominator of its own batch.
With a whitelist in place such a name is a hard error instead.

\code{prefix_strip} entries are removed longest-first, so a short label
cannot consume a longer one that starts with the same characters.
}
\examples{
g <- cr_filename_grammar(
  tokens = list(interval = "[0-9]+min", dose = "[0-9]+uM",
                mode = "treated|vehicle"),
  defaults = list(interval = "none", dose = "vehicle"),
  core_patterns = c("^vehicle$",
                    "^[0-9]+min_vehicle$",
                    "^[0-9]+uM_treated$",
                    "^[0-9]+min_[0-9]+uM_treated$"),
  prefix_strip = c("CompoundA", "CompoundB")
)
g
}
\seealso{
\code{\link[=cr_parse_paths]{cr_parse_paths()}}, \code{\link[=cr_path_spec]{cr_path_spec()}}.

Other import:
\code{\link[=cr_assign_units]{cr_assign_units()}},
\code{\link[=cr_centroid_overlap]{cr_centroid_overlap()}},
\code{\link[=cr_column_map]{cr_column_map()}},
\code{\link[=cr_extract_markers]{cr_extract_markers()}},
\code{\link[=cr_marker_rules]{cr_marker_rules()}},
\code{\link[=cr_merge_rules]{cr_merge_rules()}},
\code{\link[=cr_parse_paths]{cr_parse_paths()}},
\code{\link[=cr_path_spec]{cr_path_spec()}},
\code{\link[=cr_read_cellprofiler]{cr_read_cellprofiler()}},
\code{\link[=cr_read_cells]{cr_read_cells()}},
\code{\link[=cr_read_design]{cr_read_design()}},
\code{\link[=cr_read_export]{cr_read_export()}},
\code{\link[=cr_read_exports]{cr_read_exports()}},
\code{\link[=cr_read_qupath]{cr_read_qupath()}},
\code{\link[=cr_read_segmantr]{cr_read_segmantr()}},
\code{\link[=cr_unit_map]{cr_unit_map()}}
}
\concept{import}
