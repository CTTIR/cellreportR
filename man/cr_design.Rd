% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors-design.R
\name{cr_design}
\alias{cr_design}
\alias{print.cr_design}
\title{Build an experimental design object}
\usage{
cr_design(
  data,
  unit = NULL,
  treatment = "treatment",
  control_level = NULL,
  batch_vars = NULL,
  levels = list(),
  keep = NULL,
  call = rlang::caller_env()
)

\method{print}{cr_design}(x, ...)
}
\arguments{
\item{data}{A data frame carrying the design columns.}

\item{unit}{Name of the column identifying the analysis unit. If
\code{NULL}, the first of \code{well}, \code{slide}, \code{well_id} or \code{unit} present in
\code{data} is used.}

\item{treatment}{Name of the treatment (group) column. Default
\code{"treatment"}.}

\item{control_level}{Optional value of \code{treatment} that is the
reference level, for example the vehicle control.}

\item{batch_vars}{Optional character vector of columns that together
define a batch.}

\item{levels}{Optional named list of factor level orders applied to
the corresponding design columns.}

\item{keep}{Optional character vector of design columns to keep. The
unit column is always kept. \code{NULL} keeps every column of \code{data}.}

\item{call}{The execution environment of the calling function. Used
for error reporting; experts only.}

\item{x}{A \code{cr_design}.}

\item{...}{Ignored.}
}
\value{
An object of class \code{cr_design}:
\describe{
\item{\code{table}}{Tibble with one row per analysis unit.}
\item{\code{unit}}{Name of the unit column.}
\item{\code{treatment}}{Name of the treatment column.}
\item{\code{control_level}}{Reference level, or \code{NULL}.}
\item{\code{batch_vars}}{Character vector of batch columns.}
\item{\code{levels}}{The factor level orders that were applied.}
}

\code{x}, invisibly.
}
\description{
A \code{cr_design} records both the design table — one row per analysis
unit — and the roles its columns play: which column identifies the
unit, which holds the treatment, which level of that treatment is the
reference, and which columns together define a batch.
}
\details{
Recording the roles alongside the table is what lets later stages
standardise each cell against the control of \emph{its own} batch rather
than against one pooled reference. A batch is normally a combination
of columns (compound, run, plate, experiment, pre-treatment interval),
which a single batch variable cannot express.

\code{data} may be a per-unit table or a per-cell / per-file table; in the
latter case it is collapsed to unique rows. If a unit maps to more
than one combination of design values the collapse is ambiguous and
the offending columns are named in the error, since silently keeping
the first row would invent a design that was never run.
}
\examples{
units <- tibble::tibble(
  well = sprintf("A\%02d", 1:6),
  treatment = rep(c("Vehicle", "CompoundA"), each = 3),
  plate = rep(c("Plate_1", "Plate_2"), 3),
  replicate = rep(1:3, 2)
)
design <- cr_design(units, control_level = "Vehicle",
                    batch_vars = c("plate"))
design
}
\seealso{
\code{\link[=cr_dataset]{cr_dataset()}}, \code{\link[=cr_build_experiment]{cr_build_experiment()}}, \code{\link[=cr_read_design]{cr_read_design()}}.

Other constructors:
\code{\link[=cr_build_experiment]{cr_build_experiment()}},
\code{\link[=cr_dataset]{cr_dataset()}},
\code{\link[=cr_validate_experiment]{cr_validate_experiment()}}
}
\concept{constructors}
