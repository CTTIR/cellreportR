% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-balance.R
\name{cr_exclude_small}
\alias{cr_exclude_small}
\title{Exclude sub-threshold objects with a data-derived cut-off}
\usage{
cr_exclude_small(
  experiment,
  var = "area",
  probs = 0.1,
  threshold = NULL,
  scope = c("pooled", "batch"),
  batch_vars = NULL
)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{var}{Name of the size column in \code{cells}. Default \code{"area"}.}

\item{probs}{Quantile of the \code{var} distribution used as the
threshold (0-1). Default \code{0.10}, i.e. the lowest tenth is
dropped. Ignored when \code{threshold} is supplied.}

\item{threshold}{Optional absolute threshold. When supplied it
overrides \code{probs} and is used for every batch.}

\item{scope}{\code{"pooled"} (default) computes a single threshold from
all cells; \code{"batch"} computes one threshold per batch.}

\item{batch_vars}{Character vector of columns (from \code{cells} or
\code{design}) that define a batch. Required when \code{scope = "batch"}.}
}
\value{
A modified \code{cr_experiment}. The realised threshold(s) are
stored in \code{metadata$exclude_small} as a tibble and summarised in
the QC log.
}
\description{
Removes objects whose segmentation size falls below a quantile of
the observed size distribution. Unlike \code{\link[=cr_qc_filter]{cr_qc_filter()}}, which
applies an absolute threshold, the cut-off here is derived from the
data, so it adapts to magnification and segmentation settings. The
realised threshold is recorded, because a data-derived cut-off is
only reproducible if the value it resolved to is reported.
}
\details{
Cells with a non-finite value in \code{var} are always removed: they
cannot be placed on either side of the threshold.

\code{scope = "pooled"} computes one threshold from all cells. That is
the right choice when the segmentation settings were shared across
the whole study. \code{scope = "batch"} computes one threshold per
combination of \code{batch_vars}, which protects a batch acquired at a
different magnification from being trimmed against a pool it does
not belong to — at the cost of a threshold that is no longer
comparable between batches.

Run this step \emph{before} \code{\link[=cr_balance_cells]{cr_balance_cells()}}: balancing changes how
many cells each unit contributes to the pool, so a threshold
computed afterwards is taken on a differently weighted
distribution.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 40)
exp2 <- cr_exclude_small(exp, var = "area", probs = 0.10)
exp2$metadata$exclude_small

# one threshold per replicate block
exp3 <- cr_exclude_small(exp, scope = "batch", batch_vars = "replicate")
exp3$metadata$exclude_small
}
\seealso{
\code{\link[=cr_balance_cells]{cr_balance_cells()}}, \code{\link[=cr_qc_filter]{cr_qc_filter()}}, \code{\link[=cr_qc_report]{cr_qc_report()}}.

Other quality control:
\code{\link[=cr_apply_gate]{cr_apply_gate()}},
\code{\link[=cr_balance_cells]{cr_balance_cells()}},
\code{\link[=cr_qc_doublets]{cr_qc_doublets()}},
\code{\link[=cr_qc_filter]{cr_qc_filter()}},
\code{\link[=cr_qc_gate]{cr_qc_gate()}},
\code{\link[=cr_qc_gate_impact]{cr_qc_gate_impact()}},
\code{\link[=cr_qc_intensity]{cr_qc_intensity()}},
\code{\link[=cr_qc_manual]{cr_qc_manual()}},
\code{\link[=cr_qc_report]{cr_qc_report()}},
\code{\link[=cr_qc_summary]{cr_qc_summary()}},
\code{\link[=print.cr_qc_gate]{print.cr_qc_gate()}}
}
\concept{quality control}
