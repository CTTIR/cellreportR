% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-audit.R
\name{cr_qc_gate_impact}
\alias{cr_qc_gate_impact}
\title{Quantify the leverage of gate exclusions}
\usage{
cr_qc_gate_impact(
  experiment,
  gate,
  value = "log2_fc",
  group_var,
  reference_level,
  comparison_levels = NULL,
  by = NULL,
  unit = NULL,
  min_units = 3,
  affected_only = TRUE
)
}
\arguments{
\item{experiment}{A \code{cr_experiment} that still contains the gated
units.}

\item{gate}{A \code{cr_qc_gate} from \code{\link[=cr_qc_gate]{cr_qc_gate()}}.}

\item{value}{Per-cell numeric column averaged to the unit level
before the contrast is taken. Default \code{"log2_fc"}.}

\item{group_var}{Column holding the arm labels of the contrast.}

\item{reference_level}{Level of \code{group_var} used as the reference
arm.}

\item{comparison_levels}{Levels contrasted against the reference.
\code{NULL} (default) uses every other level present.}

\item{by}{Optional character vector of columns to compute the
contrasts within (for example the compound).}

\item{unit}{Analysis unit column. Defaults to the gate's unit.}

\item{min_units}{Minimum number of units per arm; contrasts below
it return \code{NA} estimates. Default \code{3}.}

\item{affected_only}{Restrict the output to \code{by} groups that
actually lost a unit. Default \code{TRUE}.}
}
\value{
A tibble with one row per \code{by} group and contrast:
unit counts in both states, \code{estimate_with_excluded},
\code{estimate_without_excluded}, their magnitudes, and whether the
magnitude or the sign changes when the unit is dropped. The
estimate is a pooled, uncorrected standardised mean difference.
}
\description{
Re-estimates every contrast affected by a gate exclusion twice —
with the failing unit retained and with it removed — so that "one
excluded unit changes the verdict" carries a number rather than a
recollection. This matters most when an excluded unit sits in the
\emph{reference} arm, where retaining it makes the untreated condition
look as though it were already responding.
}
\details{
Call this \strong{before} \code{\link[=cr_apply_gate]{cr_apply_gate()}}: the "retained" state needs
the failing units to still be present in \code{experiment}.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 40)
exp$cells$signal <- log2(exp$cells$marker_1)
gate <- cr_qc_gate(exp, "marker_1", "Untreated", batch_vars = "replicate")
cr_qc_gate_impact(exp, gate,
  value = "signal",
  group_var = "treatment",
  reference_level = "Untreated"
)
}
\seealso{
\code{\link[=cr_qc_gate]{cr_qc_gate()}}, \code{\link[=cr_apply_gate]{cr_apply_gate()}}.

Other quality control:
\code{\link[=cr_apply_gate]{cr_apply_gate()}},
\code{\link[=cr_balance_cells]{cr_balance_cells()}},
\code{\link[=cr_exclude_small]{cr_exclude_small()}},
\code{\link[=cr_qc_doublets]{cr_qc_doublets()}},
\code{\link[=cr_qc_filter]{cr_qc_filter()}},
\code{\link[=cr_qc_gate]{cr_qc_gate()}},
\code{\link[=cr_qc_intensity]{cr_qc_intensity()}},
\code{\link[=cr_qc_manual]{cr_qc_manual()}},
\code{\link[=cr_qc_report]{cr_qc_report()}},
\code{\link[=cr_qc_summary]{cr_qc_summary()}},
\code{\link[=print.cr_qc_gate]{print.cr_qc_gate()}}
}
\concept{quality control}
