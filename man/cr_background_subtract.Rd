% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{cr_background_subtract}
\alias{cr_background_subtract}
\title{Subtract background from a channel}
\usage{
cr_background_subtract(
  experiment,
  channel,
  method = c("percentile", "modal", "empty_wells"),
  q = 0.05,
  empty_wells = NULL
)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{channel}{Channel name.}

\item{method}{Background estimator. \code{"percentile"} (default)
uses a low percentile (default 5th), \code{"modal"} uses the mode of
a kernel density estimate, and \code{"empty_wells"} uses the median
intensity of wells listed in \code{empty_wells}.}

\item{q}{Percentile for \code{"percentile"} method (0-1, default 0.05).}

\item{empty_wells}{Character vector of well IDs for
\code{"empty_wells"}.}
}
\value{
A modified \code{cr_experiment}.
}
\description{
Computes a per-well background estimate and subtracts it from
every cell in that well. Negative values are clipped to zero.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 30)
cr_background_subtract(exp, channel = "marker_1")
}
