% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-screen.R
\name{cr_plot_screen}
\alias{cr_plot_screen}
\title{Distribution figure with the unit of replication overlaid}
\usage{
cr_plot_screen(
  cells,
  value = "log2_fc",
  group_var = "treatment",
  units = NULL,
  unit_value = NULL,
  facet_by = NULL,
  colour_by = NULL,
  reference = 0,
  seed = NULL,
  title = NULL,
  subtitle = NULL,
  x_lab = NA,
  y_lab = NULL
)
}
\arguments{
\item{cells}{Cell-level data: a data frame, or a \code{cr_experiment} whose
cells are joined to its design.}

\item{value}{Name of the cell-level value column, typically a standardised
value such as a log fold change (default \code{"log2_fc"}).}

\item{group_var}{Name of the column defining the x-axis groups (default
\code{"treatment"}).}

\item{units}{Unit-level data. Either a data frame of one row per unit, or
the name of a column in \code{cells} identifying the unit, in which case the
per-unit means are computed. \code{NULL} draws the violins alone.}

\item{unit_value}{Name of the unit-level value column. Defaults to \code{value}.}

\item{facet_by}{One or two column names to facet on, or \code{NULL}.}

\item{colour_by}{Column mapped to fill and shape. \code{NULL} (default) draws a
single colour, because hue that encodes nothing is decoration.}

\item{reference}{Horizontal reference line, or \code{NULL} for none (default
\code{0}, the null of a log fold change).}

\item{seed}{Integer seed for the point jitter, so the figure reproduces.}

\item{title, subtitle, x_lab, y_lab}{Plot labels. \code{NULL} uses a computed
default; \code{NA} omits the label.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws width-scaled violins of the cell-level value for each group, with the
per-unit means overplotted as jittered points. Cells are the observations;
the unit (well, slide or merged acquisition) is the unit of replication, and
drawing both in one panel is what keeps a reader from reading cell counts as
replicate counts.
}
\details{
The point layer is drawn with a fixed jitter seed so the figure is
reproducible, and the subtitle states which layer is the unit of
replication rather than leaving it to the caption.
}
\examples{
set.seed(1)
cells <- data.frame(
  treatment = rep(c("Vehicle", "CompoundA", "CompoundB"), each = 120),
  well_id = rep(paste0("W", 1:18), each = 20),
  log2_fc = c(rnorm(120, 0, 0.5), rnorm(120, -0.8, 0.5),
              rnorm(120, -0.2, 0.5))
)
cr_plot_screen(cells, group_var = "treatment", units = "well_id", seed = 1)
}
\seealso{
\code{\link[=cr_plot_forest]{cr_plot_forest()}}, \code{\link[=cr_theme]{cr_theme()}}, \code{\link[=cr_scale_group]{cr_scale_group()}}

Other screen figures:
\code{\link[=cr_plot_forest]{cr_plot_forest()}},
\code{\link[=cr_plot_qc_gate]{cr_plot_qc_gate()}},
\code{\link[=cr_plot_sample_size]{cr_plot_sample_size()}},
\code{\link[=cr_plot_specificity]{cr_plot_specificity()}},
\code{\link[=cr_save_plot]{cr_save_plot()}}
}
\concept{screen figures}
