% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors-design.R
\name{cr_dataset}
\alias{cr_dataset}
\alias{print.cr_dataset}
\alias{summary.cr_dataset}
\title{Build an ingested data set}
\usage{
cr_dataset(
  cells,
  design = NULL,
  unit_var = NULL,
  provenance = NULL,
  file_col = "source_path",
  metadata = list(),
  call = rlang::caller_env()
)

\method{print}{cr_dataset}(x, ...)

\method{summary}{cr_dataset}(object, ...)
}
\arguments{
\item{cells}{A data frame of cells, typically from
\code{\link[=cr_read_exports]{cr_read_exports()}}.}

\item{design}{Optional \code{\link[=cr_design]{cr_design()}} object, or a data frame that is
passed to \code{\link[=cr_design]{cr_design()}}.}

\item{unit_var}{Name of the analysis unit column. Defaults to the
design's unit column, or the first of \code{well}, \code{slide}, \code{well_id} or
\code{unit} present in \code{cells}.}

\item{provenance}{Optional per-file table. When \code{NULL} it is derived
from \code{cells}: one row per file with its cell count and every column
that is constant within the file.}

\item{file_col}{Name of the file column used for provenance. Default
\code{"source_path"}.}

\item{metadata}{Optional list of arbitrary user metadata.}

\item{call}{The execution environment of the calling function. Used
for error reporting; experts only.}

\item{x}{A \code{cr_dataset}.}

\item{...}{Ignored.}

\item{object}{A \code{cr_dataset}.}
}
\value{
An object of class \code{cr_dataset}:
\describe{
\item{\code{cells}}{Tibble of per-cell measurements.}
\item{\code{provenance}}{Tibble with one row per source file, or \code{NULL}.}
\item{\code{design}}{A \code{cr_design}, or \code{NULL}.}
\item{\code{unit_var}}{Name of the analysis unit column, or \code{NULL}.}
\item{\code{metadata}}{List of user metadata.}
}

\code{x}, invisibly.

A tibble with one row per source file, or \code{NULL} when the data
set carries no provenance.
}
\description{
A \code{cr_dataset} is what comes out of ingest: the cells of every export,
the per-file provenance that lets any row be traced back to the
acquisition it came from, and optionally the design those files
encode. It is the object to inspect before committing to an analysis,
and it converts to a \code{cr_experiment} with \code{\link[=cr_build_experiment]{cr_build_experiment()}}.
}
\examples{
cells <- tibble::tibble(
  source_path = rep(c("a.csv", "b.csv"), each = 3),
  source_file = rep(c("a.csv", "b.csv"), each = 3),
  well = rep(c("A01", "A02"), each = 3),
  treatment = rep(c("Vehicle", "CompoundA"), each = 3),
  target_signal = c(10, 12, 11, 30, 33, 29)
)
ds <- cr_dataset(cells)
ds
ds$provenance
}
\seealso{
\code{\link[=cr_read_exports]{cr_read_exports()}}, \code{\link[=cr_design]{cr_design()}}, \code{\link[=cr_build_experiment]{cr_build_experiment()}}.

Other constructors:
\code{\link[=cr_build_experiment]{cr_build_experiment()}},
\code{\link[=cr_design]{cr_design()}},
\code{\link[=cr_validate_experiment]{cr_validate_experiment()}}
}
\concept{constructors}
