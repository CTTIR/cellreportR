% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{cr_render_report}
\alias{cr_render_report}
\title{Render a report to HTML or PDF}
\usage{
cr_render_report(
  report,
  output_dir = tempdir(),
  format = c("html", "pdf", "docx"),
  template = NULL,
  title = NULL,
  author = NULL,
  quiet = TRUE
)
}
\arguments{
\item{report}{A \code{cr_report} from \code{\link[=cr_report]{cr_report()}}. A \code{cr_experiment} is
also accepted and is assembled into a report first.}

\item{output_dir}{Output directory (default \code{tempdir()}).}

\item{format}{One of \code{"html"}, \code{"pdf"} or \code{"docx"}.}

\item{template}{Path to an R Markdown template. If \code{NULL}, the
bundled template is used.}

\item{title, author}{Override the report title and author. \code{NULL}
keeps the values stored in the report.}

\item{quiet}{Passed to \code{rmarkdown::render()}.}
}
\value{
Path to the rendered document (invisibly).
}
\description{
Renders an assembled report through an R Markdown template. The
bundled template covers experimental setup, the QC log, an intensity
overview, the comparison summary and a session-info appendix. A
custom template receives the report through its \code{params}; only the
parameters a template declares are passed, so templates of different
vintages keep working.
}
\examples{
\donttest{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 10)
rep <- cr_report(exp)
if (requireNamespace("rmarkdown", quietly = TRUE) &&
    requireNamespace("knitr", quietly = TRUE) &&
    rmarkdown::pandoc_available()) {
  out <- cr_render_report(rep, output_dir = tempdir())
  basename(out)
}
}
}
\seealso{
\code{\link[=cr_report]{cr_report()}}.

Other reporting:
\code{\link[=cr_export_plots]{cr_export_plots()}},
\code{\link[=cr_export_results]{cr_export_results()}},
\code{\link[=cr_export_tables]{cr_export_tables()}},
\code{\link[=cr_report]{cr_report()}},
\code{\link[=cr_table_qc]{cr_table_qc()}},
\code{\link[=cr_tables]{cr_tables()}}
}
\concept{reporting}
