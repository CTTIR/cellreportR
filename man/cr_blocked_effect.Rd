% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics-models.R
\name{cr_blocked_effect}
\alias{cr_blocked_effect}
\title{Block-stratified sensitivity fit}
\usage{
cr_blocked_effect(
  data,
  value,
  group_var,
  reference_level,
  comparison_levels = NULL,
  block_var,
  by = NULL,
  unit = NULL,
  conf_level = 0.95,
  min_blocks = 2
)
}
\arguments{
\item{data}{A data frame with one row per unit (or per cell when
\code{unit} is given), or a \code{cr_experiment}.}

\item{value}{Name of the numeric response column.}

\item{group_var}{Name of the column holding the compared groups.}

\item{reference_level}{Reference level of \code{group_var}.}

\item{comparison_levels}{Levels contrasted against the reference.
\code{NULL} (default) uses every other level present.}

\item{block_var}{Name of the blocking column, for example the plate.}

\item{by}{Optional character vector of stratifying columns, for
example the compound.}

\item{unit}{Optional column identifying the unit of replication.
When given, rows are averaged per unit before the model is fitted.}

\item{conf_level}{Confidence level for the coefficient interval.}

\item{min_blocks}{Minimum number of blocks that must carry data for
a contrast to be fitted (default 2; with one block the model is
the unblocked one).}
}
\value{
A tibble with the \code{by} columns, \code{contrast}, \code{n_units},
\code{n_blocks}, \code{shift_within_block} (the group coefficient),
\code{ci_low}, \code{ci_high}, \code{resid_sd}, \code{d_within_block} and \code{p_value}.
Contrasts that could not be fitted are returned in the \code{skipped}
attribute.
}
\description{
Re-estimates each contrast \emph{within} a blocking factor, typically the
plate. Units on one plate share a preparation run, an imaging
session and a single control denominator, so they are not
independent and a pooled estimate carries the between-plate variance
inside its standard deviation. Fitting \code{value ~ group + block} on
unit-level data removes the additive block effect and shows whether
the pooled headline estimate is conservative or inflated by pooling.
}
\details{
The reported \code{d_within_block} is the group coefficient divided by
the residual standard deviation of the blocked fit, so it is on the
same scale as Cohen's \emph{d} from \code{\link[=cr_effect_grid]{cr_effect_grid()}} but conditions on
the block.
}
\examples{
set.seed(1)
units <- data.frame(
  compound = rep(c("CompoundA", "CompoundB"), each = 16),
  arm = rep(rep(c("reference", "interval_short"), each = 8), 2),
  plate = rep(rep(c("P1", "P2"), each = 4), 8),
  log2_fc = stats::rnorm(32)
)
units$log2_fc <- units$log2_fc +
  ifelse(units$compound == "CompoundA" & units$arm == "interval_short",
         -1.2, 0) +
  ifelse(units$plate == "P2", 0.5, 0)
cr_blocked_effect(units, value = "log2_fc", group_var = "arm",
                  reference_level = "reference",
                  block_var = "plate", by = "compound")

}
\seealso{
\code{\link[=cr_effect_grid]{cr_effect_grid()}}, \code{\link[=cr_unit_variability]{cr_unit_variability()}}.

Other screen statistics:
\code{\link[=cr_unit_variability]{cr_unit_variability()}}
}
\concept{screen statistics}
