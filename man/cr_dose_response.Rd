% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dose-response.R
\name{cr_dose_response}
\alias{cr_dose_response}
\title{Fit a dose-response curve}
\usage{
cr_dose_response(
  experiment,
  channel,
  treatment = NULL,
  model = c("4pl", "3pl", "linear"),
  log_dose = TRUE
)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{channel}{Channel name.}

\item{treatment}{Character. Names of treatments to include (for
example a compound at several doses). If \code{NULL}, the entire
design is used after filtering by \code{dose > 0}.}

\item{model}{\code{"4pl"}, \code{"3pl"} or \code{"linear"}.}

\item{log_dose}{If \code{TRUE}, the fit is done on log10 doses. Doses
must all be positive in that case.}
}
\value{
A list with class \code{"cr_dose_response"} containing the
fitted model, the data used for the fit, the estimated
parameters, and helper predictions.
}
\description{
Fits a dose-response model to well-level summaries of a treatment
across doses. Supports 4-parameter log-logistic (\code{"4pl"}),
3-parameter log-logistic with fixed lower asymptote at zero
(\code{"3pl"}), and a simple linear model (\code{"linear"}).
}
\details{
The 4PL model fitted is:
\deqn{y = d + \frac{a - d}{1 + (x / e)^b}}
where \code{a} is the top asymptote, \code{d} the bottom asymptote, \code{e} the
inflection point (EC50 / IC50), and \code{b} the Hill slope.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 30)
# Add a synthetic dose-response sub-design
exp$design$dose <- ifelse(exp$design$treatment == "CompoundA_high",
                          500, exp$design$dose)
fit <- cr_dose_response(exp, channel = "marker_1", model = "4pl")
print(fit$params)
}
