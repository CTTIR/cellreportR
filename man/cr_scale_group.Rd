% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-design.R
\name{cr_scale_group}
\alias{cr_scale_group}
\title{Grouping scales with redundant encoding}
\usage{
cr_scale_group(
  aesthetics = c("colour", "fill", "shape"),
  name = NULL,
  guide_for = NULL,
  drop = TRUE,
  mode = c("colour", "grayscale")
)
}
\arguments{
\item{aesthetics}{Character vector of aesthetics to scale. Any of
\code{"colour"}, \code{"color"}, \code{"fill"}, \code{"shape"}.}

\item{name}{Legend title. \code{NULL} (default) keeps the mapped variable name.}

\item{guide_for}{Aesthetics that keep a visible guide. Use \code{"all"} when the
aesthetics are drawn by the same layer and should merge into one key,
\code{"none"} to suppress every guide. Defaults to the last of \code{aesthetics},
which is the one drawn on top.}

\item{drop}{Passed to the underlying scales: drop unused factor levels
(default \code{TRUE}).}

\item{mode}{Colour or grayscale palette for colour/fill aesthetics.}
}
\value{
A list of \code{ggplot2} scales.
}
\description{
Builds the discrete scales that carry a grouping variable: a
colour-vision-safe hue from \code{\link[=cr_palette]{cr_palette()}} plus a matching symbol from
\code{\link[=cr_shapes]{cr_shapes()}}. Add the result to a plot with \code{+}, exactly like a single
scale.
}
\details{
Because the same variable is mapped to several aesthetics, the guide is
emitted for one aesthetic only. A duplicate guide is what makes a composed
figure print the same legend twice, so \code{guide_for} names the single
aesthetic that keeps its legend and every other aesthetic is suppressed.
}
\examples{
df <- data.frame(g = rep(c("a", "b", "c"), each = 10), y = rnorm(30))
ggplot2::ggplot(df, ggplot2::aes(g, y, colour = g, shape = g)) +
  ggplot2::geom_point() +
  cr_scale_group(c("colour", "shape"), name = "group") +
  cr_theme()
}
\seealso{
\code{\link[=cr_palette]{cr_palette()}}, \code{\link[=cr_shapes]{cr_shapes()}}, \code{\link[=cr_theme]{cr_theme()}}

Other plot design:
\code{\link[=cr_palette]{cr_palette()}},
\code{\link[=cr_shapes]{cr_shapes()}},
\code{\link[=cr_theme]{cr_theme()}}
}
\concept{plot design}
