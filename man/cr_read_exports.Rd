% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{cr_read_exports}
\alias{cr_read_exports}
\title{Read a directory tree of segmented single-cell exports}
\usage{
cr_read_exports(
  root,
  pattern = "\\\\.(csv|tsv|xls|xlsx)$",
  column_map = NULL,
  spec = NULL,
  parser = NULL,
  recursive = TRUE,
  drop_empty_rows = TRUE,
  col_types = NULL,
  progress = TRUE,
  call = rlang::caller_env()
)
}
\arguments{
\item{root}{Directory to walk.}

\item{pattern}{Regular expression selecting export files. Default
matches \code{.csv}, \code{.tsv}, \code{.xls} and \code{.xlsx}.}

\item{column_map}{Optional \code{\link[=cr_column_map]{cr_column_map()}} applied to every export.}

\item{spec}{Optional \code{\link[=cr_path_spec]{cr_path_spec()}} describing the directory and
file-name grammar. Ignored when \code{parser} is supplied.}

\item{parser}{Optional function of the file paths returning a tibble
with one row per path.}

\item{recursive}{Logical. Walk sub-directories. Default \code{TRUE}.}

\item{drop_empty_rows}{Logical. Passed to \code{\link[=cr_read_export]{cr_read_export()}}.}

\item{col_types}{Optional column-type specification passed to
\code{\link[=cr_read_export]{cr_read_export()}}.}

\item{progress}{Logical. Show a progress bar while reading. Default
\code{TRUE}.}

\item{call}{The execution environment of the calling function. Used
for error reporting; experts only.}
}
\value{
A tibble of all cells, with \code{source_file} and \code{source_path}
provenance columns and any columns produced by the parser. The paths
that were read are attached as the \code{"files"} attribute.
}
\description{
Walks a directory recursively, reads every file matching \code{pattern}
with \code{\link[=cr_read_export]{cr_read_export()}} and row-binds the result. Design facts that
live in the directory layout and in the file names — rather than
inside the files — are recovered by a parser and joined back onto the
cells.
}
\details{
No naming convention is hardcoded. Either supply a \code{\link[=cr_path_spec]{cr_path_spec()}}
via \code{spec}, which is handed to \code{\link[=cr_parse_paths]{cr_parse_paths()}}, or supply an
arbitrary \code{parser} function. A parser receives the character vector of
file paths and must return one row per path; if it returns a
\code{source_path} column the join is made on that column, otherwise row
order is assumed to match.

Row-binding is tolerant of exports that carry different subsets of the
available columns: absent columns are filled with \code{NA}.
}
\examples{
# Build a small two-file export tree.
root <- file.path(tempdir(), "cr_exports_demo")
unlink(root, recursive = TRUE)
leaf <- file.path(root, "Run1", "CompoundA", "Plate_1")
dir.create(leaf, recursive = TRUE, showWarnings = FALSE)
one <- function(n) {
  data.frame("Event Label" = seq_len(n),
             "Signal - Mean Intensity" = seq_len(n) * 10,
             check.names = FALSE)
}
utils::write.csv(one(3), file.path(leaf, "CompoundA_vehicle_1.csv"),
                 row.names = FALSE)
utils::write.csv(one(4), file.path(leaf, "CompoundA_5min_10uM_treated_1.csv"),
                 row.names = FALSE)

spec <- cr_path_spec(
  levels = c("run", "compound", "plate"),
  grammar = cr_filename_grammar(
    tokens = list(interval = "[0-9]+min", dose = "[0-9]+uM"),
    defaults = list(interval = "none", dose = "vehicle"),
    prefix_strip = "CompoundA"
  )
)
cells <- cr_read_exports(root, spec = spec, progress = FALSE)
cells
}
\seealso{
\code{\link[=cr_read_export]{cr_read_export()}}, \code{\link[=cr_path_spec]{cr_path_spec()}}, \code{\link[=cr_parse_paths]{cr_parse_paths()}},
\code{\link[=cr_dataset]{cr_dataset()}}.

Other import:
\code{\link[=cr_assign_units]{cr_assign_units()}},
\code{\link[=cr_centroid_overlap]{cr_centroid_overlap()}},
\code{\link[=cr_column_map]{cr_column_map()}},
\code{\link[=cr_extract_markers]{cr_extract_markers()}},
\code{\link[=cr_filename_grammar]{cr_filename_grammar()}},
\code{\link[=cr_marker_rules]{cr_marker_rules()}},
\code{\link[=cr_merge_rules]{cr_merge_rules()}},
\code{\link[=cr_parse_paths]{cr_parse_paths()}},
\code{\link[=cr_path_spec]{cr_path_spec()}},
\code{\link[=cr_read_cellprofiler]{cr_read_cellprofiler()}},
\code{\link[=cr_read_cells]{cr_read_cells()}},
\code{\link[=cr_read_design]{cr_read_design()}},
\code{\link[=cr_read_export]{cr_read_export()}},
\code{\link[=cr_read_qupath]{cr_read_qupath()}},
\code{\link[=cr_read_segmantr]{cr_read_segmantr()}},
\code{\link[=cr_unit_map]{cr_unit_map()}}
}
\concept{import}
