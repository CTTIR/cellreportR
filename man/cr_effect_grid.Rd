% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics-grid.R
\name{cr_effect_grid}
\alias{cr_effect_grid}
\title{Effect sizes for a whole grid of contrasts}
\usage{
cr_effect_grid(
  data,
  value,
  group_var,
  reference_level,
  comparison_levels = NULL,
  by = NULL,
  unit = NULL,
  methods = c("cohens_d", "hedges_g", "cliffs_delta"),
  conf_level = 0.95,
  min_n = 3,
  test = c("t", "wilcox", "none"),
  p_adjust = c("bonferroni", "BH"),
  level = NULL
)
}
\arguments{
\item{data}{A data frame (one row per cell or per unit) or a
\code{cr_experiment}, in which case the design is joined onto the
cells first.}

\item{value}{Name of the numeric response column, for example a
standardized log2 fold change or a raw channel.}

\item{group_var}{Name of the column holding the compared groups.}

\item{reference_level}{Level of \code{group_var} used as the reference
arm of every contrast.}

\item{comparison_levels}{Levels contrasted against the reference.
\code{NULL} (default) uses every other level present, in factor-level
order when \code{group_var} is a factor.}

\item{by}{Optional character vector of stratifying columns. One set
of contrasts is computed per combination, for example one per
compound.}

\item{unit}{Optional name of the column identifying the unit of
replication. When given, rows are averaged to one value per unit
before the contrast is computed.}

\item{methods}{Effect sizes to compute; see \code{\link[=cr_effect_size]{cr_effect_size()}}.}

\item{conf_level}{Confidence level for the intervals.}

\item{min_n}{Minimum number of observations required in \emph{each} arm.
Contrasts below it are skipped and recorded in the \code{skipped}
attribute. Use 3 for unit level and something like 10 for cell
level.}

\item{test}{P-value to accompany the effect sizes: \code{"t"} (Welch
two-sample t-test, the default), \code{"wilcox"} or \code{"none"}.}

\item{p_adjust}{Multiplicity adjustments applied across the grid.
Default both \code{"bonferroni"} and \code{"BH"}; see \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{level}{Optional label written into the \code{level} column.
Defaults to \code{"unit"} when \code{unit} is given and \code{"cell"} otherwise.}
}
\value{
A tibble with one row per stratum and contrast: the \code{by}
columns, \code{contrast}, \code{level}, \code{n_ref}, \code{n_cmp}, \code{mean_shift}, one
\verb{<method>}, \verb{<method>_ci_low}, \verb{<method>_ci_high} and
\verb{<method>_magnitude} triplet per requested method, \code{p_value}, one
\verb{p_<adjustment>} column per entry of \code{p_adjust}, and
\code{ci_excludes_zero} for the first method. The attributes
\code{conf_level}, \code{level}, \code{primary_method} and \code{skipped} (a tibble of
the contrasts that fell below \code{min_n}) are attached.
}
\description{
Drives \code{\link[=cr_effect_size]{cr_effect_size()}} over every group in a screen: for each
stratum given by \code{by}, each comparison level of \code{group_var} is
contrasted against \code{reference_level}, and every requested effect
size is reported with a confidence interval. The same function
serves both aggregation levels: supply \code{unit} to aggregate the rows
to one value per experimental unit (the unit of replication) before
the contrast is computed, or leave it \code{NULL} to work on the rows as
they are (typically single cells).
}
\details{
Cell-level intervals are anticonservative by construction, because
cells within a unit are not independent. Compute both levels and
pass them to \code{\link[=cr_compare_levels]{cr_compare_levels()}} to quantify the difference;
interpretation belongs to the unit level.

Multiplicity adjustment is applied across the entire returned grid
and reported \emph{alongside} the unadjusted p-value, never in place of
it: for an exploratory screen the effect sizes and their intervals
are the reportable quantity.
}
\examples{
set.seed(1)
units <- data.frame(
  compound = rep(c("CompoundA", "CompoundB"), each = 12),
  arm = rep(rep(c("reference", "interval_short"), each = 6), 2),
  unit_id = paste0("u", 1:24),
  log2_fc = c(stats::rnorm(6, 0, 0.3), stats::rnorm(6, -1, 0.3),
              stats::rnorm(6, 0, 0.3), stats::rnorm(6, -0.1, 0.3))
)
eff <- cr_effect_grid(units, value = "log2_fc", group_var = "arm",
                      reference_level = "reference", by = "compound")
eff[, c("compound", "contrast", "cohens_d", "cohens_d_ci_low",
        "cohens_d_ci_high", "ci_excludes_zero")]

}
\seealso{
\code{\link[=cr_effect_size]{cr_effect_size()}}, \code{\link[=cr_compare_levels]{cr_compare_levels()}},
\code{\link[=cr_power_grid]{cr_power_grid()}}, \code{\link[=cr_blocked_effect]{cr_blocked_effect()}}.

Other effect sizes:
\code{\link[=cr_compare_levels]{cr_compare_levels()}},
\code{\link[=cr_effect_size]{cr_effect_size()}}
}
\concept{effect sizes}
