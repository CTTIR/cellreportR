% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-qc.R
\name{cr_plot_qc_gate}
\alias{cr_plot_qc_gate}
\title{Quality-control gate diagnostic plot}
\usage{
cr_plot_qc_gate(
  gate,
  statistic = NULL,
  reference = NULL,
  fails_median = NULL,
  fails_mean = NULL,
  direction = c("greater", "less"),
  log_scale = TRUE,
  label = NULL,
  title = NULL,
  subtitle = NULL,
  x_lab = NULL,
  y_lab = NULL
)
}
\arguments{
\item{gate}{A \code{cr_qc_gate} object, a list carrying a \code{units} data frame, or
a data frame with one row per unit.}

\item{statistic}{Name of the unit statistic column. \code{NULL} (default)
auto-detects.}

\item{reference}{Name of the control statistic column. \code{NULL} (default)
auto-detects.}

\item{fails_median, fails_mean}{Names of the logical verdict columns under
the two centres. \code{NULL} (default) auto-detects; when neither is present
the verdict is computed from \code{statistic}, \code{reference} and \code{direction}.}

\item{direction}{\code{"greater"} (default) when a unit must exceed its control
to pass, \code{"less"} when it must fall below.}

\item{log_scale}{Draw both axes on a log scale when every value is
positive (default \code{TRUE}).}

\item{label}{Optional column whose values annotate the failing units.}

\item{title, subtitle, x_lab, y_lab}{Plot labels. \code{NULL} uses a computed
default; \code{NA} omits the label.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots each unit's statistic against the statistic of the control it is
gated on, with the line of equality. Points above the line pass a
\code{"greater"} gate; points below it fail and are excluded.
}
\details{
Where the input carries a verdict under both centres, median and mean, the
units whose verdict \emph{depends} on which centre was used are drawn as their
own class. That distinction matters because a right-skewed signal has
a mean above its median, so gating a raw median against a control mean is
silently stricter than the stated rule, and a gate that can only drop low
units manufactures apparent effects when it is too strict.
}
\examples{
set.seed(1)
units <- data.frame(
  well_id = paste0("W", 1:24),
  unit_median = c(rlnorm(20, 5, 0.3), rlnorm(4, 3.9, 0.2)),
  ctrl_median = rlnorm(24, 4.2, 0.1)
)
cr_plot_qc_gate(units, label = "well_id")
}
\seealso{
\code{\link[=cr_plot_qc]{cr_plot_qc()}} for the distribution dashboard.

Other screen figures:
\code{\link[=cr_plot_forest]{cr_plot_forest()}},
\code{\link[=cr_plot_sample_size]{cr_plot_sample_size()}},
\code{\link[=cr_plot_screen]{cr_plot_screen()}},
\code{\link[=cr_plot_specificity]{cr_plot_specificity()}},
\code{\link[=cr_save_plot]{cr_save_plot()}}
}
\concept{screen figures}
