% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-balance.R
\name{cr_balance_cells}
\alias{cr_balance_cells}
\title{Balance the number of cells per analysis unit}
\usage{
cr_balance_cells(experiment, unit = NULL, n_max = NULL, n = NULL, seed = NULL)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{unit}{Column in \code{cells} identifying the analysis unit.
Defaults to the experiment's unit column, falling back to its
spatial unit (\code{well} / \code{slide}).}

\item{n_max}{Optional maximum number of cells per unit.}

\item{n}{Optional exact number of cells per unit. Cannot be
combined with \code{n_max}.}

\item{seed}{Optional integer seed.}
}
\value{
A modified \code{cr_experiment}. Per-unit counts before and
after are stored in \code{metadata$balance_cells}.
}
\description{
Randomly subsamples each analysis unit to a common cell count. A
unit acquired in several passes otherwise contributes several times
as many cells as its single-pass neighbour and is silently
over-weighted in every pooled quantity they share — including the
control statistics of their own batch.
}
\details{
Three modes:
\describe{
\item{\code{n_max}}{cap each unit at \code{n_max} cells, leaving smaller
units untouched. This is the usual choice for large
acquisitions.}
\item{\code{n}}{take exactly \code{n} cells per unit (units with fewer
cells keep all of theirs).}
\item{neither}{take the smallest unit's cell count from every
unit, which equalises the units exactly at the cost of
discarding the most data.}
}

Subsampling is random, so pass \code{seed} for a reproducible result.
The RNG state of the calling session is saved and restored, so a
seeded stream outside this function is never disturbed.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 40)
exp2 <- cr_balance_cells(exp, n_max = 20, seed = 1)
max(exp2$metadata$balance_cells$n_after)

# equalise every unit at the smallest unit's count
exp3 <- cr_balance_cells(exp, seed = 1)
length(unique(exp3$metadata$balance_cells$n_after))
}
\seealso{
\code{\link[=cr_exclude_small]{cr_exclude_small()}}, which should run first.

Other quality control:
\code{\link[=cr_apply_gate]{cr_apply_gate()}},
\code{\link[=cr_exclude_small]{cr_exclude_small()}},
\code{\link[=cr_qc_doublets]{cr_qc_doublets()}},
\code{\link[=cr_qc_filter]{cr_qc_filter()}},
\code{\link[=cr_qc_gate]{cr_qc_gate()}},
\code{\link[=cr_qc_gate_impact]{cr_qc_gate_impact()}},
\code{\link[=cr_qc_intensity]{cr_qc_intensity()}},
\code{\link[=cr_qc_manual]{cr_qc_manual()}},
\code{\link[=cr_qc_report]{cr_qc_report()}},
\code{\link[=cr_qc_summary]{cr_qc_summary()}},
\code{\link[=print.cr_qc_gate]{print.cr_qc_gate()}}
}
\concept{quality control}
