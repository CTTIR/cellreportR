% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example-exports.R
\name{cr_example_exports}
\alias{cr_example_exports}
\title{Write a synthetic export tree}
\usage{
cr_example_exports(
  dir = tempdir(),
  seed = 42,
  n_cells = 20,
  format = c("csv", "xlsx")
)
}
\arguments{
\item{dir}{Directory to write the tree into. Created when it does not
exist.}

\item{seed}{Random seed. \code{NULL} uses the current RNG state.}

\item{n_cells}{Number of cells written per file.}

\item{format}{File format, \code{"csv"} (default) or \code{"xlsx"}. Writing
\code{"xlsx"} needs the \code{writexl} package.}
}
\value{
A character vector of the written file paths, invisibly.
}
\description{
Writes a nested directory of single-acquisition export files whose
design information lives in the directory layout and the file names
rather than inside the files. This is the input the multi-file ingest
functions were written for, so that \code{\link[=cr_read_exports]{cr_read_exports()}},
\code{\link[=cr_parse_paths]{cr_parse_paths()}} and \code{\link[=cr_extract_markers]{cr_extract_markers()}} can be demonstrated on
real files.
}
\details{
The layout below \code{dir} is
\verb{Run1/<compound>/<experiment>/<plate>/<file>}, and the file names
follow the grammar \verb{<compound>_<interval>_<dose>_<mode>_<replicate>}
in which an absent token is meaningful: a name carrying no exposure
token is a vehicle control. Parenthetical markers are attached to
three files -- a two-pass acquisition, a repeated read and a
reagent-omitted acquisition -- and one directory is marked as a partly
filled plate.

Column headers are raw instrument names rather than analysis names,
and one of them carries a unit glyph, so that a \code{\link[=cr_column_map]{cr_column_map()}} is
genuinely required to read them.
}
\examples{
d <- file.path(tempdir(), "cr_example_exports")
files <- cr_example_exports(d, seed = 1, n_cells = 5)
basename(files)

map <- cr_column_map(
  exact = c("Event Label" = "cell_id",
            "Target - Signal Mean" = "target_signal"),
  prefix = c("^Nuclei - Area" = "area")
)
cells <- cr_read_exports(d, column_map = map, progress = FALSE)
head(cells[, c("source_file", "cell_id", "target_signal", "area")], 3)
}
\seealso{
\code{\link[=cr_read_exports]{cr_read_exports()}}, \code{\link[=cr_path_spec]{cr_path_spec()}}, \code{\link[=cr_example_screen]{cr_example_screen()}},
\code{\link[=cr_example_path]{cr_example_path()}}.

Other example data:
\code{\link[=cr_example_design]{cr_example_design()}},
\code{\link[=cr_example_experiment]{cr_example_experiment()}},
\code{\link[=cr_example_files]{cr_example_files()}},
\code{\link[=cr_example_path]{cr_example_path()}},
\code{\link[=cr_example_screen]{cr_example_screen()}}
}
\concept{example data}
