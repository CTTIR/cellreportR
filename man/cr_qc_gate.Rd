% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc-gate.R
\name{cr_qc_gate}
\alias{cr_qc_gate}
\title{Gate analysis units against their own in-batch control}
\usage{
cr_qc_gate(
  experiment,
  channel,
  control_level,
  batch_vars = NULL,
  unit = NULL,
  control_var = "treatment",
  statistic = c("median", "mean"),
  reference = c("median", "mean"),
  direction = c("greater", "less"),
  gate_controls = FALSE,
  min_cells = 1L
)
}
\arguments{
\item{experiment}{A \code{cr_experiment}.}

\item{channel}{Name of the raw signal column in \code{cells}.}

\item{control_level}{Value of \code{control_var} marking the vehicle
control units.}

\item{batch_vars}{Character vector of columns (from \code{cells} or
\code{design}) whose combination defines one batch. \code{NULL} (default)
treats the whole experiment as a single batch.}

\item{unit}{Column identifying the analysis unit. Defaults to the
experiment's unit column, falling back to its spatial unit.}

\item{control_var}{Column holding the treatment labels. Default
\code{"treatment"}.}

\item{statistic}{Centre of the \emph{unit} used for the comparison:
\code{"median"} (default) or \code{"mean"}.}

\item{reference}{Centre of the \emph{control} used as the threshold:
\code{"median"} (default) or \code{"mean"}.}

\item{direction}{\code{"greater"} (default) requires a unit to exceed
its control; \code{"less"} requires it to fall below.}

\item{gate_controls}{Should control units be gated against
themselves? Default \code{FALSE}; they form the reference arm.}

\item{min_cells}{Units with fewer than \code{min_cells} cells fail
regardless of their signal. Default \code{1}, i.e. no effect.}
}
\value{
An object of class \code{cr_qc_gate}, a list with:
\describe{
\item{\code{units}}{One row per analysis unit: cell count, both raw
centres, the control statistics of its batch,
\code{pct_of_control}, \code{fails_vs_median}, \code{fails_vs_mean},
\code{disputed}, \code{verdict} and \code{reason}.}
\item{\code{disputed}}{The units whose verdict depends on which
control centre is used.}
\item{\code{excluded}}{Identifiers of the units that fail under the
chosen \code{statistic}/\code{reference} combination.}
\item{\code{params}}{The arguments the gate was built with.}
}
}
\description{
A biological rather than a statistical gate: a treated unit must
carry more target signal than the vehicle control acquired in its
own batch. Units that do not are candidates for exclusion. The
comparison is made against the control of the unit's \emph{own} batch,
so a plate that ran hot or cold as a whole cannot pass or fail
units in unrelated batches.
}
\details{
Two properties of this gate decide whether it is honest, and both
are made explicit here.

\strong{Like for like.} Target signal is usually right-skewed, so a
control's mean sits above its median. Comparing a unit's median
against the control's \emph{mean} is therefore silently stricter than
the rule it claims to apply. Because the gate can only ever drop
\emph{low} units, an over-strict gate manufactures apparent treatment
effects. Both verdicts are computed for every unit —
\code{fails_vs_median} and \code{fails_vs_mean} — and the units whose verdict
\emph{depends} on that choice are returned in \verb{$disputed}. The default
(\code{statistic = "median"}, \code{reference = "median"}) is like for like.

\strong{Leverage.} Excluding a unit changes the estimate it fed into.
Quantify that with \code{\link[=cr_qc_gate_impact]{cr_qc_gate_impact()}} \emph{before} calling
\code{\link[=cr_apply_gate]{cr_apply_gate()}}, while both states are still in hand.

Gate the analysis unit, not the acquisition file: a unit assembled
from two files would otherwise be gated twice.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 40)
gate <- cr_qc_gate(exp,
  channel = "marker_1",
  control_level = "Untreated",
  batch_vars = "replicate"
)
gate
head(gate$units[, c("well", "n_cells", "pct_of_control", "verdict")])
gate$excluded
}
\seealso{
\code{\link[=cr_qc_gate_impact]{cr_qc_gate_impact()}}, \code{\link[=cr_apply_gate]{cr_apply_gate()}},
\code{\link[=cr_qc_report]{cr_qc_report()}}.

Other quality control:
\code{\link[=cr_apply_gate]{cr_apply_gate()}},
\code{\link[=cr_balance_cells]{cr_balance_cells()}},
\code{\link[=cr_exclude_small]{cr_exclude_small()}},
\code{\link[=cr_qc_doublets]{cr_qc_doublets()}},
\code{\link[=cr_qc_filter]{cr_qc_filter()}},
\code{\link[=cr_qc_gate_impact]{cr_qc_gate_impact()}},
\code{\link[=cr_qc_intensity]{cr_qc_intensity()}},
\code{\link[=cr_qc_manual]{cr_qc_manual()}},
\code{\link[=cr_qc_report]{cr_qc_report()}},
\code{\link[=cr_qc_summary]{cr_qc_summary()}},
\code{\link[=print.cr_qc_gate]{print.cr_qc_gate()}}
}
\concept{quality control}
