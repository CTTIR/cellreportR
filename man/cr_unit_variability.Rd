% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics-models.R
\name{cr_unit_variability}
\alias{cr_unit_variability}
\title{Between-unit variability within a condition}
\usage{
cr_unit_variability(data, value, by, unit = NULL, min_units = 2, log_base = 2)
}
\arguments{
\item{data}{A data frame with one row per unit (or per cell when
\code{unit} is given), or a \code{cr_experiment}.}

\item{value}{Name of the numeric response column, typically a
log-scale fold change.}

\item{by}{Character vector of columns defining a condition, for
example compound, experiment, plate and pre-treatment interval.}

\item{unit}{Optional column identifying the unit of replication.
When given, rows are averaged per unit first.}

\item{min_units}{Minimum number of units a group must contain to be
reported (default 2).}

\item{log_base}{Base of the logarithm the response is on. Used to
turn the spread into a fold range. Default 2.}
}
\value{
A tibble with the \code{by} columns, \code{n_units}, \code{mean_value},
\code{sd_value}, \code{cv_pct}, \code{spread} and \code{fold_range}, carrying a
\code{summary} attribute (a one-row tibble with the number of groups
and the median, quartiles and maximum of the fold range).
}
\description{
Summarizes how far apart the units of one condition sit. For every
group defined by \code{by} that holds at least \code{min_units} units, the
number of units, the coefficient of variation, the spread
(\code{max - min} of the unit values) and the fold range
(\code{log_base ^ spread}) are reported.
}
\details{
This is unit-to-unit variability. It is \strong{not} within-unit
technical repeatability: that would require the same unit to be
measured twice, which a design with one acquisition per unit cannot
deliver. A unit assembled from two partial acquisitions is two
halves of one unit, not two reads of it.

The coefficient of variation is computed as
\code{100 * sd / abs(mean)}, which is unstable when the mean of a
log-scale response approaches zero; the fold range is the robust
summary and is what the attached overall summary reports.
}
\examples{
set.seed(1)
units <- data.frame(
  compound = rep(c("CompoundA", "CompoundB"), each = 12),
  plate = rep(rep(c("P1", "P2"), each = 6), 2),
  log2_fc = stats::rnorm(24, 0, 0.4)
)
v <- cr_unit_variability(units, value = "log2_fc",
                         by = c("compound", "plate"))
v
attr(v, "summary")

}
\seealso{
\code{\link[=cr_blocked_effect]{cr_blocked_effect()}}, \code{\link[=cr_effect_grid]{cr_effect_grid()}}.

Other screen statistics:
\code{\link[=cr_blocked_effect]{cr_blocked_effect()}}
}
\concept{screen statistics}
