% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize-batch.R
\name{cr_batch_key}
\alias{cr_batch_key}
\title{Construct a batch key}
\usage{
cr_batch_key(x, batch_vars, sep = " | ", na_label = "<NA>")
}
\arguments{
\item{x}{A \code{cr_experiment} or a data frame. For a \code{cr_experiment}
the design columns are joined onto the cells first; a column
present in both tables is taken from \code{cells}.}

\item{batch_vars}{Character vector of column names that jointly
define a batch, for example
\code{c("compound", "run", "plate", "experiment", "interval")}.}

\item{sep}{Separator placed between the parts of the key.}

\item{na_label}{Label substituted for missing values.}
}
\value{
A character vector with one key per row of \code{x} (one key per
cell when \code{x} is a \code{cr_experiment}).
}
\description{
A batch is rarely identified by a single column. Once an assay
spans several plates, acquisition runs or experiment days, the
analytical batch is the \emph{combination} of those columns, and any
analysis that standardizes against a control has to respect that
combination. \code{cr_batch_key()} collapses any number of design or
cell columns into one character key that can be grouped on, joined
on and reported.
}
\details{
Missing values are replaced by \code{na_label} rather than propagated,
so that two rows with an unrecorded plate are grouped together
visibly instead of silently producing \code{NA} keys.
}
\examples{
exp <- cr_example_experiment(seed = 1, n_cells_per_well = 10)
exp$design$plate <- rep(c("P1", "P2"), length.out = nrow(exp$design))
key <- cr_batch_key(exp, c("plate", "timepoint"))
head(unique(key))

# Works on a plain data frame too
cr_batch_key(data.frame(a = c("x", "y"), b = 1:2), c("a", "b"))
}
\seealso{
\code{\link[=cr_batch_reference]{cr_batch_reference()}}, \code{\link[=cr_standardize_batch]{cr_standardize_batch()}}

Other batch standardization functions:
\code{\link[=cr_batch_reference]{cr_batch_reference()}},
\code{\link[=cr_correct_batch]{cr_correct_batch()}},
\code{\link[=cr_standardize_batch]{cr_standardize_batch()}}
}
\concept{batch standardization functions}
