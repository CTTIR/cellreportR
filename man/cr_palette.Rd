% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-design.R
\name{cr_palette}
\alias{cr_palette}
\title{Colour-vision-safe palette}
\usage{
cr_palette(
  n = NULL,
  type = c("qualitative", "sequential", "diverging"),
  names = NULL,
  mode = c("colour", "grayscale")
)
}
\arguments{
\item{n}{Number of colours to return. \code{NULL} (default) returns the whole
palette: 8 colours for \code{"qualitative"}, 256 for the continuous types.
Requesting more than 8 qualitative colours interpolates between the
anchors and warns, because the result is no longer guaranteed to be
colour-vision-safe; prefer facetting over more than 8 hues.}

\item{type}{One of \code{"qualitative"} (discrete groups), \code{"sequential"}
(one-directional magnitude) or \code{"diverging"} (signed magnitude around a
meaningful zero, ramped blue-to-vermillion rather than blue-to-red-green).}

\item{names}{Optional character vector of names to attach to the returned
colours, for use as the \code{values} argument of a manual scale. Must have
the same length as the result.}

\item{mode}{\code{"colour"} or a monotonically separated \code{"grayscale"} palette.}
}
\value{
A character vector of hex colour strings, named when \code{names} is
supplied or when the whole qualitative palette is returned.
}
\description{
Returns hex colours from the palette every \verb{cr_plot_*} function uses for
discrete groupings. The qualitative set is Okabe-Ito, the standard
colour-vision-safe qualitative palette; it deliberately contains no
red/green pair, so hue never encodes an opposition that a large minority of
readers cannot separate.
}
\details{
Hue alone is not treated as a sufficient encoding: use \code{\link[=cr_shapes]{cr_shapes()}} (or
\code{\link[=cr_scale_group]{cr_scale_group()}}, which wires both at once) so that every grouping is
carried by shape as well as colour.
}
\examples{
cr_palette()
cr_palette(3)
cr_palette(5, names = c("a", "b", "c", "d", "e"))
cr_palette(7, type = "diverging")
}
\seealso{
\code{\link[=cr_shapes]{cr_shapes()}}, \code{\link[=cr_scale_group]{cr_scale_group()}}, \code{\link[=cr_theme]{cr_theme()}}

Other plot design:
\code{\link[=cr_scale_group]{cr_scale_group()}},
\code{\link[=cr_shapes]{cr_shapes()}},
\code{\link[=cr_theme]{cr_theme()}}
}
\concept{plot design}
